---
phase: 07-auto-run-extended-analysis-after-scrape
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - static/js/stockScraper.js
autonomous: false
requirements:
  - AUTO-01
  - AUTO-02
  - AUTO-03

must_haves:
  truths:
    - "User clicks Run Analysis, sees Stocks tab, then Analytics tab auto-switches and shows Running badges"
    - "After scrape displayResults() completes, triggerAutoRun is called with the scraped ticker list"
    - "Analytics tab is activated before auto-run starts so user sees badges updating live"
    - "Badge transitions are visible to the user without any page reload"
  artifacts:
    - path: "static/js/stockScraper.js"
      provides: "Auto-run trigger wired into displayResults()"
      contains: "AutoRun.triggerAutoRun"
  key_links:
    - from: "static/js/stockScraper.js displayResults()"
      to: "window.AutoRun.triggerAutoRun"
      via: "called after TabManager.switchTab('analytics') with AppState.currentTickers"
      pattern: "AutoRun\\.triggerAutoRun"
---

<objective>
Wire AutoRun.triggerAutoRun() into stockScraper.js displayResults() so auto-run fires after every scrape, then validate the full end-to-end flow with a human-verify checkpoint.

Purpose: One-line wiring change makes the auto-run module activate. The checkpoint catches any integration issues (wrong tab switching order, badge not appearing, chart container ID collision) before the phase is declared complete.

Output: static/js/stockScraper.js (two-line addition in displayResults)
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-auto-run-extended-analysis-after-scrape/07-CONTEXT.md
@.planning/phases/07-auto-run-extended-analysis-after-scrape/07-01-SUMMARY.md

Relevant stockScraper.js section (lines 131-171):

displayResults(result) fires after every successful scrape. Current final lines:
  TabManager.switchTab('stocks');
  document.getElementById('resultsSection').classList.add('active');

The auto-run wiring must:
1. Switch to Analytics tab INSTEAD OF Stocks tab (per user decision: auto-switch to Analytics so badges are visible)
2. Call AutoRun.triggerAutoRun(AppState.currentTickers) AFTER switching tab

AppState.currentTickers is set at line 106 before displayResults() is called, so it is available.
AutoRun is defined on window by autoRun.js (loaded before main.js).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AutoRun.triggerAutoRun into stockScraper.js displayResults()</name>
  <files>static/js/stockScraper.js</files>
  <action>
In static/js/stockScraper.js, locate displayResults() (around line 131). Find the two final lines:
  TabManager.switchTab('stocks');
  document.getElementById('resultsSection').classList.add('active');

Replace TabManager.switchTab('stocks') with TabManager.switchTab('analytics').
After document.getElementById('resultsSection').classList.add('active'), add:

  // Auto-run extended analysis (regime detection + portfolio MDP)
  if (window.AutoRun) {
      AutoRun.triggerAutoRun(AppState.currentTickers);
  }

This is the complete change. Do not modify any other part of stockScraper.js.

Rationale for if (window.AutoRun) guard: prevents hard crash if autoRun.js fails to load for any reason.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const src=fs.readFileSync('static/js/stockScraper.js','utf8');const checks=['AutoRun.triggerAutoRun','window.AutoRun','AppState.currentTickers'];const missing=checks.filter(c=>!src.includes(c));if(missing.length){console.error('MISSING in stockScraper.js:',missing);process.exit(1);}if(src.includes(\"switchTab('stocks')\")&&!src.includes('switchTab'+'('+'\"analytics\"'+')')){console.error('Still switching to stocks tab instead of analytics');process.exit(1);}console.log('stockScraper.js wiring: OK');"</automated>
  </verify>
  <done>stockScraper.js contains AutoRun.triggerAutoRun(AppState.currentTickers) call; tab switches to analytics not stocks after scrape</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Full auto-run pipeline: after clicking Run Analysis, the app switches to the Analytics tab showing "Auto Analysis" section with per-ticker "Regime Detection — TICKER Running..." badges, and (for 2+ tickers) a "Portfolio MDP (SPY / IEF) Running..." badge. Badges update to "Done" or "Failed" as each API call resolves.
  </what-built>
  <how-to-verify>
1. Start the Flask app: python webapp.py (or however you run it locally)
2. Enter two tickers, e.g., AAPL and MSFT, click "Run Analysis"
3. Confirm the app auto-switches to the Analytics tab (not Stocks)
4. Confirm "Auto Analysis" section appears at the TOP of the Analytics tab with:
   - "Regime Detection — AAPL  Rolling..." badge (grey)
   - "Regime Detection — MSFT  Rolling..." badge (grey)
   - "Portfolio MDP (SPY / IEF)  Running..." badge (grey)
5. Wait for badges to resolve (typically 10-30s per ticker). Confirm:
   - Each resolved badge shows "Done" (green) or "Failed" (red) — not stuck on Running
   - Done badges have Plotly charts rendered beneath them (probability time series + price with shading)
   - MDP section shows a cumulative return line chart and V* bar chart (or "Failed" badge if API error)
6. Run again with a SINGLE ticker (e.g., SPY only):
   - Confirm "Auto Analysis" section shows only the one regime detection block
   - Confirm NO Portfolio MDP block appears (not even a Failed badge — it is fully absent)
7. Confirm the manual Stochastic Models > Regime Detection tab still works independently — its charts still render in #regimeProbChart and #regimePriceChart (not affected by auto-run IDs)
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Automated check on stockScraper.js for AutoRun wiring
2. Human verification of full end-to-end flow (7-point checklist above)
</verification>

<success_criteria>
- stockScraper.js calls AutoRun.triggerAutoRun(AppState.currentTickers) after every scrape
- App auto-switches to Analytics tab (not Stocks) after scrape completes
- Human confirms: badges appear and transition, charts render, single-ticker MDP absent, manual tab unaffected
- Phase 7 requirements AUTO-01 through AUTO-05 are all observable in the running app
</success_criteria>

<output>
After completion, create .planning/phases/07-auto-run-extended-analysis-after-scrape/07-02-SUMMARY.md
</output>
