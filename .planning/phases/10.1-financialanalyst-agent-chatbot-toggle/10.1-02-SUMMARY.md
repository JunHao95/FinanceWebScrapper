---
phase: "10.1"
plan: "02"
subsystem: "frontend-chatbot"
tags: ["agent-toggle", "pill-tabs", "chat-history", "css", "javascript"]
dependency_graph:
  requires: ["10.1-01"]
  provides: ["pill-tab-toggle", "per-agent-history", "agent-field-in-fetch"]
  affects: ["static/css/styles.css", "static/js/chatbot.js"]
key_files:
  created: []
  modified:
    - "static/css/styles.css"
    - "static/js/chatbot.js"
decisions:
  - "Event delegation on #chatbot-agent-tabs — single listener handles both pills"
  - "replayMode param on appendMessage() prevents double-pushing history on replay"
  - "appendTypingIndicator() does NOT push to agentHistories — transient indicators excluded from history"
metrics:
  completed: "2026-03-20"
  tasks_completed: 3
  files_changed: 2
---

# Plan 10.1-02 Summary: Pill-tab Agent Toggle + Per-agent History

**One-liner:** Rewrote chatbot.js with two-agent pill toggle, per-agent in-memory history swap, and `agent` field in fetch; appended pill tab CSS to styles.css. Human-verified in browser.

## What Was Built

### CSS pill tabs (static/css/styles.css)

Appended to end of file — no existing rules modified:
- `#chatbot-header` — flex layout (title-info | tabs | close btn)
- `.agent-pill` — ghost pill with rgba dark-theme palette
- `.agent-pill.active` — brightened background + white text
- `.agent-pill:hover:not(.active)` — subtle hover lift

### chatbot.js rewrite (static/js/chatbot.js)

| Feature | Implementation |
|---------|---------------|
| Agent config | `AGENT_CONFIG` with name, subtitle, greeting per agent |
| Per-agent history | `agentHistories = { quant: [], financial: [] }` |
| Write-through | `appendMessage(sender, text, replayMode=false)` pushes to history when `!replayMode` |
| Header update | `updateHeader(agent)` syncs h3, subtitle, pill `.active` class |
| Agent switch | `switchAgent(newAgent)` clears DOM, replays history or seeds greeting |
| Fetch body | `JSON.stringify({ message: text, agent: activeAgent })` |

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| CSS | 6ed834b | feat(10.1-02): add pill tab CSS for agent toggle |
| JS | 293d416 | feat(10.1-02): rewrite chatbot.js with agent toggle and per-agent history |

## Human Verification: APPROVED

## Self-Check: PASSED
