---
phase: 21-order-flow
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_trading_indicators.py
  - src/analytics/trading_indicators.py
  - webapp.py
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
must_haves:
  truths:
    - "compute_order_flow returns a dict with keys: traces, layout, signal, divergence"
    - "cumulative delta series contains no NaN values — epsilon guard on zero-range bars is applied"
    - "volume divergence flag is True when price slope and volume slope have opposite signs over a 10-bar window"
    - "imbalance candle annotations are present in the layout when body > 70% of range AND volume > 1.2x 20-day average"
    - "GET /api/trading_indicators?ticker=AAPL&lookback=90 returns order_flow with real traces (not status: stub)"
  artifacts:
    - path: "tests/test_trading_indicators.py"
      provides: "TestComputeOrderFlow test class with 7 test methods"
      contains: "TestComputeOrderFlow"
    - path: "src/analytics/trading_indicators.py"
      provides: "Full compute_order_flow implementation replacing stub"
      exports: ["compute_order_flow"]
    - path: "webapp.py"
      provides: "Route wired to compute_order_flow"
      contains: "compute_order_flow(df, ticker, lookback)"
  key_links:
    - from: "webapp.py"
      to: "src/analytics/trading_indicators.py"
      via: "from src.analytics.trading_indicators import ... compute_order_flow"
      pattern: "compute_order_flow"
    - from: "compute_order_flow"
      to: "_safe_list"
      via: "NaN sanitisation before JSON serialisation"
      pattern: "_safe_list"
---

<objective>
Implement the Order Flow backend: write failing tests first (RED), then implement `compute_order_flow` to pass them (GREEN), then wire the route in `webapp.py`.

Purpose: All three FLOW requirements depend on a correct Python implementation. Tests lock the behaviour contract before code is written, preventing NaN propagation bugs and misfire conditions from reaching the browser.
Output: A tested `compute_order_flow` function returning `{traces, layout, signal, divergence}`, stubbed route replaced, full test suite green.
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-order-flow/21-CONTEXT.md
@.planning/phases/21-order-flow/21-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/analytics/trading_indicators.py (line 402 — current stub to replace):
```python
def compute_order_flow(df: pd.DataFrame) -> dict:
    return {'status': 'stub'}
```

Existing helpers already in module (DO NOT recreate):
```python
PAPER_BG = '#1e1e2e'
PLOT_BG  = '#1e1e2e'
FONT_CLR = '#cdd6f4'

def _safe_list(series: pd.Series) -> list:
    # Already exists at line 237 — converts NaN → None for JSON safety
    ...

# Existing signature patterns for reference:
def compute_volume_profile(df: pd.DataFrame, ticker: str = '', lookback: int = 0) -> dict: ...
def compute_anchored_vwap(df: pd.DataFrame, ticker: str, lookback: int) -> dict: ...
```

From webapp.py (lines 2161–2169 — import + stub to update):
```python
from src.analytics.trading_indicators import fetch_ohlcv, compute_volume_profile, compute_anchored_vwap
# ...
'order_flow': {'status': 'stub'},
```

From tests/test_trading_indicators.py — existing test class patterns (for fixture style):
```python
class TestComputeVolumeProfile:
    def test_...(self):
        df = _make_ohlcv(...)   # synthetic DataFrame fixture
        result = compute_volume_profile(df)
        assert ...

class TestComputeAnchoredVwap:
    ...
```
</interfaces>
</context>

<feature>
  <name>compute_order_flow — Order Flow delta chart, divergence signal, imbalance annotations</name>
  <files>tests/test_trading_indicators.py, src/analytics/trading_indicators.py, webapp.py</files>
  <behavior>
    - FLOW-01 / test_no_nan_in_cumulative_delta: Build synthetic OHLCV including one doji bar (High==Low). Call compute_order_flow(df). Assert no NaN in the cumulative delta series embedded in traces[1]['y'].
    - FLOW-01 / test_epsilon_guard_on_zero_range: Build a 1-row DataFrame where Open=Close=High=Low=100, Volume=1000. Assert result['signal'] is 'bullish' or 'bearish' (not NaN/error).
    - FLOW-01 / test_order_flow_keys: Build synthetic 30-row OHLCV. Assert result has keys: 'traces', 'layout', 'signal', 'divergence'. Assert result['divergence'] has keys: 'detected', 'price_slope', 'vol_slope'.
    - FLOW-02 / test_divergence_detected_opposite_slopes: Build 15-row OHLCV where Close is rising (+0.5/bar) and Volume is falling (-100/bar). Assert result['divergence']['detected'] is True.
    - FLOW-02 / test_no_divergence_same_sign_slopes: Build 15-row OHLCV where both Close and Volume are monotonically rising. Assert result['divergence']['detected'] is False.
    - FLOW-03 / test_imbalance_candle_annotation: Build 25-row OHLCV with one bar where: Open=100, Close=108 (body=8, range=8 → 100% body ratio), Volume=1000 while avg_vol_20=100 (10x spike). Assert annotations list in result['layout']['annotations'] contains at least one entry with text='▲'.
    - FLOW-03 / test_no_annotation_normal_candle: Build 25-row OHLCV with all bars at body_ratio=0.3 and volume at 1× average. Assert result['layout']['annotations'] is empty list.
  </behavior>
  <implementation>
RED: Append TestComputeOrderFlow class to tests/test_trading_indicators.py with all 7 stubs. Run `pytest tests/test_trading_indicators.py::TestComputeOrderFlow -x -q` — must FAIL (ImportError or assertion failure). Commit: `test(21-01): add failing TestComputeOrderFlow tests`.

GREEN: Replace the `compute_order_flow` stub at trading_indicators.py:402 with full implementation per RESEARCH.md Code Examples. Add `ticker: str = ''` and `lookback: int = 0` parameters matching existing function signatures. Key implementation steps:
  1. Epsilon guard: `ranges = (df['High'] - df['Low']).clip(lower=1e-9)`
  2. Delta: `buy_ratio = (df['Close'] - df['Low']) / ranges`, `delta = (2 * buy_ratio - 1) * df['Volume']`
  3. Cumulative delta: `cumulative_delta = delta.cumsum()`
  4. Bar colors: `['#2ecc71' if v >= 0 else '#e74c3c' for v in delta]` (treat zero as buy-neutral)
  5. Rolling slope helper `_last_slope(series)` using `np.polyfit(x, y, 1)[0]` on last 10 bars
  6. Divergence: `detected = (price_slope * vol_slope < 0) and (len(df) >= 10)`
  7. Imbalance: `body_ratio > 0.70 AND volume > 1.2 × rolling(20, min_periods=1).mean()`
  8. Annotations: `yref='y'` (NOT 'y2') with `▲` for bullish at `delta_values[i] + offset`, `▼` for bearish at `delta_values[i] - offset`
  9. Dual-axis layout: `go.Bar` on `yaxis='y'`, `go.Scatter` cumulative on `yaxis='y2'` with `overlaying='y', side='right'`
  10. Zero line: shapes dict `type='line', xref='paper', x0=0, x1=1, yref='y', y0=0, y1=0, dash='dash'`
  11. Use `_safe_list()` on both delta and cumulative_delta before adding to traces
  12. `d = fig.to_dict(); d['layout'].pop('template', None)`
  13. Return `{traces: d['data'], layout: d['layout'], signal: ..., divergence: {...}}`

Update webapp.py:
  - Extend import: `from src.analytics.trading_indicators import fetch_ohlcv, compute_volume_profile, compute_anchored_vwap, compute_order_flow`
  - Replace `'order_flow': {'status': 'stub'}` with `'order_flow': compute_order_flow(df, ticker, lookback)`
  
Run `pytest tests/test_trading_indicators.py::TestComputeOrderFlow -x -q` — must PASS. Commit: `feat(21-01): implement compute_order_flow with delta, divergence, imbalance`.
  </implementation>
</feature>

<verification>
<automated>pytest tests/test_trading_indicators.py::TestComputeOrderFlow -x -q</automated>
Full suite check: `pytest tests/ -x -q`
Route smoke test: `curl -s "http://localhost:5001/api/trading_indicators?ticker=AAPL&lookback=90" | python3 -c "import sys,json; d=json.load(sys.stdin); of=d['order_flow']; assert 'traces' in of, 'still stub'; print('OK - traces count:', len(of['traces'])); print('divergence:', of['divergence'])"`
</verification>

<success_criteria>
- All 7 TestComputeOrderFlow tests pass
- Full test suite green (`pytest tests/ -x -q`)
- `GET /api/trading_indicators?ticker=AAPL&lookback=90` returns `order_flow` with `traces` (list, not stub)
- `order_flow.divergence` contains `detected`, `price_slope`, `vol_slope` keys
- No NaN values appear in `order_flow.traces[1].y` (cumulative delta)
</success_criteria>

<output>
After completion, create `.planning/phases/21-order-flow/21-01-SUMMARY.md`
</output>
