---
phase: 25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_unit_codebase_health.py
  - tests/test_integration_routes.py
  - src/indicators/technical_indicators.py
  - src/utils/data_formatter.py
  - src/utils/comparison_utils.py
  - src/utils/email_utils.py
  - src/scrapers/finviz_scraper.py
  - src/sentiment/sentiment_analyzer.py
  - src/scrapers/cnn_scraper.py
  - src/analytics/credit_transitions.py
  - static/js/stockScraper.js
  - webapp.py
autonomous: true
requirements:
  - BUG-01
  - BUG-02
  - BUG-03
must_haves:
  truths:
    - "No bare print() calls remain in webapp.py or src/ source files"
    - "document.getElementById in stockScraper.js resolves to the correct drawer element"
    - "percentile_rank returns values across the full 0–100 range, not always 50"
  artifacts:
    - path: "tests/test_unit_codebase_health.py"
      provides: "Unit tests for BUG-01, BUG-03 fixes"
      exports: ["test_no_debug_prints", "test_percentile_rank"]
    - path: "tests/test_integration_routes.py"
      provides: "Integration test stubs for SEC-02, SEC-03, SEC-04 (Wave 0 stubs)"
    - path: "webapp.py"
      provides: "Fixed percentile_rank using bisect (both copies)"
      contains: "bisect.bisect_left"
  key_links:
    - from: "tests/test_unit_codebase_health.py"
      to: "webapp.py"
      via: "imports webapp and inspects percentile_rank behavior"
    - from: "static/js/stockScraper.js"
      to: "templates/index.html"
      via: "getElementById matches actual element id='settings-drawer'"
---

<objective>
Create the test scaffold for all Phase 25 fixes, then apply all three bug fixes: remove debug print() calls from src/ modules, fix the broken JS advanced-settings element reference, and fix percentile_rank with bisect-based logic.

Purpose: Establish automated verification before changing production code (Wave 0 Nyquist requirement), then close all three confirmed bugs that cause silent failures in production.
Output: Test files covering all Phase 25 items, src/ modules free of print() calls, correct JS drawer ID, working percentile rank computation.
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins/25-RESEARCH.md

<interfaces>
<!-- From src/indicators/technical_indicators.py (confirmed print locations) -->
<!-- Lines 1051–1052: print(20*"###") and print("No historical data...") -->
<!-- Fix: silent removal (not worth logging) -->

<!-- From src/utils/data_formatter.py lines 113, 166 -->
<!-- print(f"Error saving to CSV/Excel") — replace with logger.error -->

<!-- From src/utils/comparison_utils.py lines 375, 395 -->
<!-- print(f"Warning: Metric...") — replace with logger.warning -->

<!-- From src/utils/email_utils.py lines 1269–1270 -->
<!-- print(f"Sending report...") — replace with logger.info -->

<!-- From src/scrapers/finviz_scraper.py lines 33, 40 -->
<!-- print(f"Analyst Price Target...") — replace with logger.debug -->

<!-- From src/sentiment/sentiment_analyzer.py lines 431, 521, 523, 525, 528, 539 -->
<!-- mix of debug and progress prints — debug → logger.debug, progress → logger.info -->

<!-- From src/scrapers/cnn_scraper.py lines 57, 61 -->
<!-- print(f"DEBUG, results...") — replace with logger.debug / logger.error -->

<!-- From src/analytics/credit_transitions.py line 318 -->
<!-- test assertion print — replace with logger.debug -->

<!-- webapp.py: no bare print() calls confirmed by research grep -->

<!-- BUG-02: stockScraper.js line 49 -->
<!-- WRONG: document.getElementById('advanced-settings') -->
<!-- CORRECT: document.getElementById('settings-drawer') -->
<!-- The drawer uses an open CSS class, not <details> .open. -->
<!-- Research note: after this fix, check how advancedDetails.open is tested -->
<!-- The drawer likely tracks open state via classList.contains('open') or similar -->
<!-- Inspect the surrounding toggle logic to use the right check -->

<!-- BUG-03: webapp.py lines ~2089–2098 and ~2123–2132 (two copies) -->
<!-- BUGGY:
def percentile_rank(rows, field, target):
    if target is None: return 50
    vals = sorted([r[field] for r in rows if r[field] is not None])
    if len(vals) < 2: return 50
    if target not in vals:   # <-- broken float comparison
        return 50
    idx = vals.index(target)
    return round(100 * idx / (len(vals) - 1))
-->
<!-- FIX (bisect approach):
import bisect

def percentile_rank(rows, field, target):
    if target is None: return 50
    vals = sorted([r[field] for r in rows if r[field] is not None])
    if len(vals) < 2: return 50
    idx = bisect.bisect_left(vals, target)
    idx = min(idx, len(vals) - 1)
    return round(100 * idx / (len(vals) - 1))
-->
<!-- CRITICAL: Both copies of percentile_rank must be updated (cached path ~2089, non-cached ~2123) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 test scaffold for all Phase 25 items</name>
  <files>tests/test_unit_codebase_health.py, tests/test_integration_routes.py</files>
  <behavior>
    - test_no_debug_prints: grep src/ and webapp.py for bare print( calls — assert count == 0
    - test_percentile_rank: call percentile_rank with rows=[{f:1},{f:2},{f:3},{f:4},{f:5}], field='f', target=3 — assert result == 50; target=5 → assert result == 100; target=1 → assert result == 0
    - test_secret_key_guard: monkeypatch os.environ to remove SECRET_KEY, attempt to import/init app — assert RuntimeError raised
    - test_cache_bounded: import webapp, assert isinstance(_peer_cache, TTLCache or LRUCache), assert _peer_cache.maxsize is not None
    - test_procfile_workers: open Procfile, assert '--workers 2' in content
    - test_requirements_pinned: open requirements.txt, assert no line matches the pattern r'\w+>='
    - test_precommit_config: assert os.path.exists('.pre-commit-config.yaml'), assert os.path.exists('.flake8')
    - test_no_duplicate_js_helpers: read healthScore.js, earningsQuality.js, dcfValuation.js — assert none define a function named parseNumeric at module scope
    - test_regime_cache: mock HMM fit, call regime_detection route twice with same params — assert fit called exactly once
    - test_email_allowlist (integration stub in test_integration_routes.py): POST /api/send_report with email not in allowlist → assert 403 response
    - test_rate_limiting (integration stub in test_integration_routes.py): mark xfail or skip until rate limiter is installed in plan 02
    - test_no_client_api_keys (integration stub in test_integration_routes.py): POST /api/scrape with alpha_key in body — assert it is ignored (route reads from env only)
  </behavior>
  <action>
    Create tests/test_unit_codebase_health.py with all unit tests listed above. Use pytest markers:
    - Mark file with pytestmark = pytest.mark.unit
    - test_no_debug_prints: use subprocess or direct file reading with re.findall for bare print( calls; exclude test files, venv, __pycache__; assert zero matches
    - test_percentile_rank: import percentile_rank directly from webapp (use importlib or direct import with app context if needed)
    - test_secret_key_guard: use monkeypatch to unset SECRET_KEY; the guard must raise RuntimeError; if app initializes at import time this may require a subprocess approach — use subprocess.run(['python', '-c', 'import webapp'], env=env_without_key) and assert returncode != 0
    - test_cache_bounded, test_procfile_workers, test_requirements_pinned, test_precommit_config, test_no_duplicate_js_helpers: file-reading assertions — these will fail until the fixes are applied in later tasks/plans, which is expected RED state
    - test_regime_cache: import webapp, mock RegimeDetector.fit using unittest.mock.patch, call the route twice, assert mock called once

    Add to tests/test_integration_routes.py the three new stubs (test_email_allowlist, test_rate_limiting, test_no_client_api_keys). Mark test_rate_limiting with @pytest.mark.skip(reason="Flask-Limiter installed in plan 02").

    Run: pytest tests/test_unit_codebase_health.py -x -q
    Expected: most tests RED (fixes not yet applied) — that is correct Wave 0 state.
  </action>
  <verify>
    <automated>pytest tests/test_unit_codebase_health.py -q --tb=no 2>&1 | tail -5</automated>
  </verify>
  <done>tests/test_unit_codebase_health.py exists with all 9 unit test functions. tests/test_integration_routes.py has 3 new stubs. Tests run without import errors (failures are expected at this stage).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix BUG-01 (debug prints), BUG-02 (JS ID), BUG-03 (percentile rank)</name>
  <files>
    src/indicators/technical_indicators.py,
    src/utils/data_formatter.py,
    src/utils/comparison_utils.py,
    src/utils/email_utils.py,
    src/scrapers/finviz_scraper.py,
    src/sentiment/sentiment_analyzer.py,
    src/scrapers/cnn_scraper.py,
    src/analytics/credit_transitions.py,
    static/js/stockScraper.js,
    webapp.py
  </files>
  <behavior>
    - BUG-01: grep -r "^\s*print(" src/ webapp.py (excluding test files) returns 0 matches after fix
    - BUG-02: stockScraper.js contains getElementById('settings-drawer') not getElementById('advanced-settings')
    - BUG-03: percentile_rank([{f:1}..{f:5}], 'f', 3) == 50, target=1 → 0, target=5 → 100
  </behavior>
  <action>
    **BUG-01 — Replace print() calls with logging in src/ files:**

    For each file, use the existing module-level `logger` (or create one with `logging.getLogger(__name__)` if absent):

    - src/indicators/technical_indicators.py lines 1051-1052: Remove both print statements (not worth logging — they are diagnostic noise around missing historical data; replace with `logger.warning("No historical data for ticker")` or remove silently)
    - src/utils/data_formatter.py lines 113, 166: Replace `print(f"Error saving...")` with `logger.error(f"Error saving...")`
    - src/utils/comparison_utils.py lines 375, 395: Replace `print(f"Warning: Metric...")` with `logger.warning(f"Warning: Metric...")`
    - src/utils/email_utils.py lines 1269-1270: Replace `print(f"Sending report...")` with `logger.info(f"Sending report...")`
    - src/scrapers/finviz_scraper.py lines 33, 40: Replace `print(f"Analyst Price Target...")` with `logger.debug(f"...")`
    - src/sentiment/sentiment_analyzer.py lines 431, 521, 523, 525, 528, 539: Replace debug prints with logger.debug, progress prints (user-facing messages) with logger.info
    - src/scrapers/cnn_scraper.py lines 57, 61: Replace `print(f"DEBUG...")` with `logger.debug(...)` and error prints with `logger.error(...)`
    - src/analytics/credit_transitions.py line 318: Replace test assertion print with `logger.debug(...)`

    For any file that lacks a module-level logger, add at the top: `import logging; logger = logging.getLogger(__name__)`

    **BUG-02 — Fix JS drawer ID:**
    In static/js/stockScraper.js line ~49, change:
      `const advancedDetails = document.getElementById('advanced-settings');`
    to:
      `const advancedDetails = document.getElementById('settings-drawer');`
    Then inspect the subsequent uses of `advancedDetails` (lines ~50-100). The drawer likely tracks open state via a CSS class, not the `<details>.open` property. If the code checks `advancedDetails.open`, change to check the appropriate open state (e.g., `advancedDetails.classList.contains('open')` or `advancedDetails.style.display !== 'none'` — inspect the drawer toggle in index.html/tabs.js to determine the correct check).

    **BUG-03 — Fix percentile_rank in webapp.py:**
    Add `import bisect` at the top of webapp.py (if not present).
    Find BOTH copies of `percentile_rank` (around lines 2089 and 2123).
    Replace the `if target not in vals: return 50` guard and `vals.index(target)` logic in BOTH copies with:
    ```python
    idx = bisect.bisect_left(vals, target)
    idx = min(idx, len(vals) - 1)
    return round(100 * idx / (len(vals) - 1))
    ```
    Remove the `if target not in vals` line entirely from both copies.
  </action>
  <verify>
    <automated>pytest tests/test_unit_codebase_health.py::test_no_debug_prints tests/test_unit_codebase_health.py::test_percentile_rank -x -q</automated>
  </verify>
  <done>test_no_debug_prints passes (zero print() calls in src/ and webapp.py). test_percentile_rank passes (returns 0, 50, 100 for min, mid, max values). stockScraper.js references 'settings-drawer'.</done>
</task>

</tasks>

<verification>
pytest tests/test_unit_codebase_health.py::test_no_debug_prints tests/test_unit_codebase_health.py::test_percentile_rank -v
grep -r "^\s*print(" src/ webapp.py --include="*.py" | grep -v test | grep -v venv | wc -l
grep "advanced-settings" static/js/stockScraper.js  # should return nothing
grep "settings-drawer" static/js/stockScraper.js    # should return a match
grep "bisect" webapp.py                              # should show bisect import and usage
</verification>

<success_criteria>
- test_no_debug_prints: PASS
- test_percentile_rank: PASS (0/50/100 for min/mid/max inputs)
- No print() calls remain in src/ or webapp.py (excluding venv/tests)
- stockScraper.js references 'settings-drawer' not 'advanced-settings'
- Both copies of percentile_rank in webapp.py use bisect.bisect_left
- Test scaffold exists for all 13 Phase 25 items (RED → GREEN progression ready)
</success_criteria>

<output>
After completion, create `.planning/phases/25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins/25-01-SUMMARY.md`
</output>
