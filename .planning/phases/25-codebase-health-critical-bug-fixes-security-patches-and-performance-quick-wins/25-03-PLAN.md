---
phase: 25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins
plan: 03
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - .flake8
  - .pre-commit-config.yaml
  - Procfile
  - static/js/utils.js
  - static/js/healthScore.js
  - static/js/earningsQuality.js
  - static/js/dcfValuation.js
  - static/js/chatbot.js
autonomous: true
requirements:
  - TECH-03
  - TECH-04
  - PERF-04
must_haves:
  truths:
    - ".flake8 and .pre-commit-config.yaml exist and are valid"
    - "Procfile uses --workers 2"
    - "parseNumeric is defined only in utils.js, not in healthScore.js / earningsQuality.js / dcfValuation.js"
    - "escapeHtml in chatbot.js calls Utils.escapeHtml() rather than a local definition"
  artifacts:
    - path: ".flake8"
      provides: "flake8 configuration with max-line-length 120"
      contains: "max-line-length"
    - path: ".pre-commit-config.yaml"
      provides: "pre-commit hooks for black and flake8"
      contains: "black"
    - path: "Procfile"
      provides: "Gunicorn with --workers 2"
      contains: "--workers 2"
    - path: "static/js/utils.js"
      provides: "Canonical parseNumeric function in Utils object"
      contains: "parseNumeric"
  key_links:
    - from: "static/js/healthScore.js"
      to: "static/js/utils.js"
      via: "calls Utils.parseNumeric() instead of local parseNumeric()"
    - from: "static/js/chatbot.js"
      to: "static/js/utils.js"
      via: "calls Utils.escapeHtml() instead of local escapeHtml()"
---

<objective>
Apply three independent tech-debt and performance items that do not touch webapp.py: add pre-commit linting config, update Gunicorn to 2 workers in Procfile, and deduplicate parseNumeric / escapeHtml JS helpers into utils.js.

Purpose: Enforce code style automatically going forward, unblock concurrent requests on Render, and eliminate four JS files each defining the same 15-line helper.
Output: .flake8 + .pre-commit-config.yaml created, Procfile updated, Utils.parseNumeric added to utils.js, local definitions removed from healthScore.js / earningsQuality.js / dcfValuation.js / chatbot.js.
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins/25-RESEARCH.md
@.planning/phases/25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins/25-01-SUMMARY.md

<interfaces>
<!-- PERF-04: Current Procfile -->
<!-- web: gunicorn webapp:app --workers 1 --timeout 600 --log-level info --access-logfile - --error-logfile - -->
<!-- Change --workers 1 to --workers 2 — only this one token changes -->

<!-- TECH-03: .flake8 content -->
<!--
[flake8]
max-line-length = 120
exclude = venv, __pycache__, .git, migrations
extend-ignore = E203, W503
-->
<!-- .pre-commit-config.yaml content -->
<!--
repos:
  - repo: https://github.com/psf/black
    rev: 25.1.0
    hooks:
      - id: black
        language_version: python3
  - repo: https://github.com/pycqa/flake8
    rev: 7.1.2
    hooks:
      - id: flake8
-->
<!-- After creating these files: pip install pre-commit && pre-commit install -->
<!-- IMPORTANT: Do NOT run pre-commit run --all-files — only changed files this phase -->

<!-- TECH-04: parseNumeric in utils.js — add to Utils object body -->
<!-- Canonical implementation (identical across all 3 source files): -->
<!--
parseNumeric(val) {
    if (val === null || val === undefined || val === '' || val === 'N/A' || val === 'N/A%') return null;
    if (typeof val === 'number') return isNaN(val) ? null : val;
    const s = String(val).trim();
    let cleaned = s.replace(/^\$/, '');
    const multipliers = { 'B': 1e9, 'M': 1e6, 'K': 1e3 };
    const lastChar = cleaned.slice(-1).toUpperCase();
    if (multipliers[lastChar]) {
        const n = parseFloat(cleaned.slice(0, -1));
        return isNaN(n) ? null : n * multipliers[lastChar];
    }
    cleaned = cleaned.replace(/%$/, '');
    const n = parseFloat(cleaned);
    return isNaN(n) ? null : n;
},
-->
<!-- utils.js already has Utils.escapeHtml at line 9 — no change needed there -->
<!-- chatbot.js line ~85: local escapeHtml function — remove and replace calls with Utils.escapeHtml() -->
<!-- healthScore.js line 16: local parseNumeric function — remove and replace calls with Utils.parseNumeric() -->
<!-- earningsQuality.js line 14: local parseNumeric function — remove and replace calls with Utils.parseNumeric() -->
<!-- dcfValuation.js line 14: local parseNumeric function — remove and replace calls with Utils.parseNumeric() -->
<!-- utils.js is loaded first (index.html line 1346) — Utils is available to all modules -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TECH-03 pre-commit config + PERF-04 Gunicorn workers</name>
  <files>.flake8, .pre-commit-config.yaml, Procfile</files>
  <action>
    **TECH-03 — Create linting config files:**

    Create .flake8 in the project root:
    ```ini
    [flake8]
    max-line-length = 120
    exclude = venv, __pycache__, .git, migrations
    extend-ignore = E203, W503
    ```

    Create .pre-commit-config.yaml in the project root:
    ```yaml
    repos:
      - repo: https://github.com/psf/black
        rev: 25.1.0
        hooks:
          - id: black
            language_version: python3
      - repo: https://github.com/pycqa/flake8
        rev: 7.1.2
        hooks:
          - id: flake8
    ```

    Install the pre-commit tool and register the hooks:
    ```bash
    pip install pre-commit
    pre-commit install
    ```

    CRITICAL: Do NOT run `pre-commit run --all-files` — this would reformat the entire codebase creating a massive unrelated diff. The hook will run automatically on future commits.

    **PERF-04 — Update Gunicorn worker count:**
    In Procfile, change `--workers 1` to `--workers 2`. Keep all other flags (--timeout 600 --log-level info --access-logfile - --error-logfile -) identical.

    Run: pytest tests/test_unit_codebase_health.py::test_precommit_config tests/test_unit_codebase_health.py::test_procfile_workers -x -q
  </action>
  <verify>
    <automated>pytest tests/test_unit_codebase_health.py::test_precommit_config tests/test_unit_codebase_health.py::test_procfile_workers -x -q</automated>
  </verify>
  <done>test_precommit_config passes (.flake8 and .pre-commit-config.yaml exist). test_procfile_workers passes (Procfile contains --workers 2). pre-commit install succeeds without error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TECH-04 deduplicate JS parseNumeric and escapeHtml helpers</name>
  <files>static/js/utils.js, static/js/healthScore.js, static/js/earningsQuality.js, static/js/dcfValuation.js, static/js/chatbot.js</files>
  <behavior>
    - After fix: healthScore.js, earningsQuality.js, dcfValuation.js contain no function named parseNumeric at module scope
    - After fix: chatbot.js contains no standalone escapeHtml function (Utils.escapeHtml calls are fine)
    - After fix: utils.js Utils object contains a parseNumeric method
    - test_no_duplicate_js_helpers: PASS
  </behavior>
  <action>
    **Step 1 — Add Utils.parseNumeric to utils.js:**
    In static/js/utils.js, inside the `const Utils = { ... }` object body, add the parseNumeric method after the existing escapeHtml method. Use the canonical implementation exactly as specified in the interfaces block above.

    **Step 2 — Remove local parseNumeric from healthScore.js:**
    Find the local `function parseNumeric(val) { ... }` (around line 16) and remove the entire function definition.
    Search for all calls to `parseNumeric(` in the file and replace with `Utils.parseNumeric(`.

    **Step 3 — Remove local parseNumeric from earningsQuality.js:**
    Same as Step 2: remove the local function definition (around line 14), replace all calls with `Utils.parseNumeric(`.

    **Step 4 — Remove local parseNumeric from dcfValuation.js:**
    Same as Step 2: remove the local function definition (around line 14), replace all calls with `Utils.parseNumeric(`.

    **Step 5 — Remove local escapeHtml from chatbot.js:**
    Find the local `function escapeHtml(text) { ... }` (around line 85) and remove the entire function definition.
    Replace all calls to `escapeHtml(` in chatbot.js with `Utils.escapeHtml(`.

    Note: analyticsRenderer.js has `escapeHtml` as a method on its own object (`this.escapeHtml`). Leave this one as-is — it is encapsulated within the object and not a duplicate at module scope.

    Verify utils.js loads before all module scripts — confirmed at index.html line 1346. No load-order change needed.

    Run: pytest tests/test_unit_codebase_health.py::test_no_duplicate_js_helpers -x -q
  </action>
  <verify>
    <automated>pytest tests/test_unit_codebase_health.py::test_no_duplicate_js_helpers -x -q</automated>
  </verify>
  <done>test_no_duplicate_js_helpers passes. Utils.parseNumeric exists in utils.js. No module-scope parseNumeric in healthScore.js, earningsQuality.js, dcfValuation.js. No standalone escapeHtml in chatbot.js.</done>
</task>

</tasks>

<verification>
pytest tests/test_unit_codebase_health.py::test_precommit_config tests/test_unit_codebase_health.py::test_procfile_workers tests/test_unit_codebase_health.py::test_no_duplicate_js_helpers -v
cat Procfile | grep "workers"              # must show --workers 2
cat .flake8 | grep "max-line-length"       # must show 120
grep "parseNumeric" static/js/utils.js    # must show definition
grep "function parseNumeric" static/js/healthScore.js  # must return nothing
grep "function parseNumeric" static/js/earningsQuality.js  # must return nothing
grep "function parseNumeric" static/js/dcfValuation.js  # must return nothing
grep "function escapeHtml" static/js/chatbot.js  # must return nothing
</verification>

<success_criteria>
- test_precommit_config: PASS
- test_procfile_workers: PASS
- test_no_duplicate_js_helpers: PASS
- .flake8 exists with max-line-length = 120
- .pre-commit-config.yaml exists with black 25.1.0 and flake8 7.1.2 hooks
- Procfile shows --workers 2
- Utils.parseNumeric defined in utils.js
- No local parseNumeric in healthScore, earningsQuality, dcfValuation
- No standalone escapeHtml in chatbot.js
</success_criteria>

<output>
After completion, create `.planning/phases/25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins/25-03-SUMMARY.md`
</output>
