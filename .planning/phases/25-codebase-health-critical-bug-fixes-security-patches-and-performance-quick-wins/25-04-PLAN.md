---
phase: 25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins
plan: 04
type: execute
wave: 3
depends_on:
  - 25-02
files_modified:
  - webapp.py
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
must_haves:
  truths:
    - "Second identical regime_detection request hits the cache and skips HMM fit"
    - "_peer_cache, _ticker_sector_map, and _ticker_validation_cache all have bounded max size"
    - "No yfinance double-fetch on repeated route calls for the same ticker"
  artifacts:
    - path: "webapp.py"
      provides: "TTLCache for regime results, LRU/TTL capped peer + sector + validation caches"
      contains: "TTLCache"
  key_links:
    - from: "webapp.py _regime_cache"
      to: "cachetools.TTLCache"
      via: "cache_key = (ticker, start_date, end_date) → TTLCache(maxsize=50, ttl=900)"
    - from: "webapp.py _peer_cache"
      to: "cachetools.TTLCache"
      via: "replaces manual fetched_at time check with TTLCache auto-expiry"
---

<objective>
Apply the three performance improvements that all live in webapp.py: add TTLCache for HMM regime results, convert the three unbounded cache dicts to cachetools LRU/TTL bounded caches, and confirm (with test) that yfinance is not double-fetched for trading indicators.

Purpose: Prevent repeated HMM fits for the same ticker/date range wasting 2–10s per request, prevent cache dicts from growing without bound consuming server memory, and verify the PERF-01 double-fetch concern is fully closed.
Output: _regime_cache is a TTLCache(maxsize=50, ttl=900), _peer_cache is TTLCache(maxsize=30, ttl=1800), _ticker_sector_map is LRUCache(maxsize=200), _ticker_validation_cache is TTLCache(maxsize=500, ttl=3600). All bounded. test_regime_cache and test_cache_bounded pass.
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins/25-RESEARCH.md
@.planning/phases/25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins/25-02-SUMMARY.md

<interfaces>
<!-- cachetools already installed: cachetools==5.5.2 — no pip install needed -->
<!-- webapp.py current cache declarations (confirmed in research): -->
<!-- _ticker_validation_cache = {}   line ~2066 — no eviction -->
<!-- _peer_cache = {}                line ~2068 — has manual TTL check but no size cap -->
<!-- _ticker_sector_map = {}         line ~2069 — no eviction -->

<!-- PERF-02 HMM cache pattern: -->
<!--
from cachetools import TTLCache, LRUCache   # add to imports at top of webapp.py

_regime_cache: TTLCache = TTLCache(maxsize=50, ttl=900)  # 15-min TTL

# Inside regime_detection route, before any HMM computation:
cache_key = (ticker, start_date, end_date)
if cache_key in _regime_cache:
    return jsonify(_regime_cache[cache_key])
# ... existing HMM fit + response building ...
_regime_cache[cache_key] = convert_numpy_types(response)
return jsonify(_regime_cache[cache_key])
-->

<!-- PERF-03 unbounded cache replacements: -->
<!--
# Replace the three dict declarations with:
_ticker_validation_cache: TTLCache = TTLCache(maxsize=500, ttl=3600)   # 1hr TTL
_peer_cache: TTLCache = TTLCache(maxsize=30, ttl=1800)                 # 30-min TTL (matches original manual check)
_ticker_sector_map: LRUCache = LRUCache(maxsize=200)

# For _peer_cache: the existing access code checks entry['fetched_at'] manually.
# TTLCache auto-expires entries — remove the manual TTL check.
# Keep reading _peer_cache[key] and writing _peer_cache[key] = value as before.
# Remove any code block that computes (now - entry['fetched_at'] < 1800) check.
# TTLCache raises KeyError on expired keys — the existing try/except or 'if key in _peer_cache' guard handles this correctly.
-->

<!-- PERF-01 double fetch: -->
<!-- Research confirmed: trading_indicators route calls fetch_ohlcv once and passes df to sub-functions -->
<!-- Sub-functions receive df and do NOT call fetch_ohlcv internally -->
<!-- The potential double-fetch is in /api/regime_detection: route fetches log_ret and passes it to detector.fit() -->
<!-- RegimeDetector.fit() takes np.ndarray — does NOT call yfinance internally -->
<!-- Conclusion: no code change needed for PERF-01 — just add the test to confirm -->
<!-- The test_no_double_fetch test in Wave 0 verifies this by mocking yf.Ticker and asserting call count = 1 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PERF-02 HMM TTL cache + PERF-03 bounded cache replacements</name>
  <files>webapp.py</files>
  <behavior>
    - test_regime_cache: two identical POST /api/regime_detection calls → RegimeDetector.fit mock called exactly once (cache hit on second call)
    - test_cache_bounded: _peer_cache.maxsize == 30, _ticker_sector_map.maxsize == 200, _ticker_validation_cache.maxsize == 500
    - test_no_double_fetch: yf.Ticker().history mock called exactly once per /api/regime_detection invocation
  </behavior>
  <action>
    **Step 1 — Add cachetools imports:**
    At the top of webapp.py, in the existing imports section, add:
    ```python
    from cachetools import TTLCache, LRUCache
    ```
    (cachetools==5.5.2 is already installed — no pip install needed)

    **Step 2 — Replace unbounded cache dicts (PERF-03):**
    Find the three cache dict declarations near lines 2066–2069 and replace:
    ```python
    # BEFORE:
    _ticker_validation_cache = {}
    _peer_cache = {}
    _ticker_sector_map = {}

    # AFTER:
    _ticker_validation_cache: TTLCache = TTLCache(maxsize=500, ttl=3600)   # 1-hr TTL, 500 tickers
    _peer_cache: TTLCache = TTLCache(maxsize=30, ttl=1800)                 # 30-min TTL, 30 sectors
    _ticker_sector_map: LRUCache = LRUCache(maxsize=200)                   # 200 tickers LRU
    ```

    For `_peer_cache`: find the manual TTL check (code that reads `entry['fetched_at']` and computes `now - entry['fetched_at'] < 1800`). Remove the manual expiry check — TTLCache handles expiry automatically. Change the storage to drop `fetched_at` from the stored dict: store `{'data': ..., 'peers': [...]}` instead of `{'data': ..., 'fetched_at': ..., 'peers': [...]}`.

    **Step 3 — Add HMM results cache (PERF-02):**
    Add a new module-level cache declaration near the other caches:
    ```python
    _regime_cache: TTLCache = TTLCache(maxsize=50, ttl=900)  # 15-min TTL, 50 ticker/range combos
    ```

    In the regime_detection route handler, find where the HMM fit and response building occurs. Add a cache lookup BEFORE the fit:
    ```python
    # Build cache key from request parameters (ticker, start_date, end_date)
    cache_key = (ticker, data.get('start_date', ''), data.get('end_date', ''))
    if cache_key in _regime_cache:
        return jsonify(_regime_cache[cache_key])
    ```

    After building the response dict and BEFORE returning, store in cache:
    ```python
    _regime_cache[cache_key] = convert_numpy_types(result_dict)
    return jsonify(_regime_cache[cache_key])
    ```

    Add a code comment: `# Note: TTLCache is per-worker (not shared across Gunicorn workers). Each worker has its own cache — this is expected and safe for in-memory caching.`

    **Step 4 — Verify PERF-01 (no double fetch — confirm, not fix):**
    No code change needed. The test_no_double_fetch unit test (created in plan 01) verifies this by mocking yf.Ticker and asserting it is called at most once per route invocation. Run the test to confirm green.

    Run: pytest tests/test_unit_codebase_health.py::test_regime_cache tests/test_unit_codebase_health.py::test_cache_bounded tests/test_unit_codebase_health.py::test_no_double_fetch -x -q
  </action>
  <verify>
    <automated>pytest tests/test_unit_codebase_health.py::test_regime_cache tests/test_unit_codebase_health.py::test_cache_bounded tests/test_unit_codebase_health.py::test_no_double_fetch -x -q</automated>
  </verify>
  <done>test_regime_cache passes (RegimeDetector.fit called once for two identical requests). test_cache_bounded passes (all three caches have maxsize set). test_no_double_fetch passes. webapp.py imports from cachetools and declares four bounded cache objects.</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite green check after webapp.py changes</name>
  <files></files>
  <action>
    Run the full test suite to confirm no regressions from the performance changes in webapp.py.

    Run: pytest -q --tb=short 2>&1 | tail -20

    If any existing test fails due to the _peer_cache structural change (fetched_at key removed):
    - Find the test that checks peer cache structure and update the assertion to match the new dict shape: {'data': ..., 'peers': [...]}
    - Do NOT revert the cache structure change — update the test

    If any test fails due to Flask-Limiter rate limiting interfering with test client requests:
    - Add `app.config["RATELIMIT_ENABLED"] = False` to the test conftest.py Flask test app setup, or
    - Add `@limiter.exempt` to routes that need to be tested without rate limiting, or
    - Use `RATELIMIT_STORAGE_URL = "memory://"` with `RATELIMIT_ENABLED = False` in test config

    Confirm: pytest exits with 0 failures (warnings are acceptable)
  </action>
  <verify>
    <automated>pytest -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>Full pytest suite passes with 0 failures. All new tests in test_unit_codebase_health.py are green. No regressions in existing test files.</done>
</task>

</tasks>

<verification>
pytest tests/test_unit_codebase_health.py -v
pytest -q --tb=short | tail -10
grep "TTLCache\|LRUCache" webapp.py | wc -l   # should be >= 6 (imports + 4 declarations + cache logic)
grep "_regime_cache" webapp.py | wc -l        # should show declaration + lookup + store
</verification>

<success_criteria>
- test_regime_cache: PASS
- test_cache_bounded: PASS
- test_no_double_fetch: PASS
- Full test suite: 0 failures
- webapp.py has TTLCache for _regime_cache, _ticker_validation_cache, _peer_cache
- webapp.py has LRUCache for _ticker_sector_map
- All four caches have explicit maxsize bounds
</success_criteria>

<output>
After completion, create `.planning/phases/25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins/25-04-SUMMARY.md`
</output>
