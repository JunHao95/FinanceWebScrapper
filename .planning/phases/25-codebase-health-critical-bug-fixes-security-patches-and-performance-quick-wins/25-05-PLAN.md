---
phase: 25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins
plan: 05
type: execute
wave: 4
depends_on:
  - 25-02
  - 25-04
files_modified:
  - requirements.txt
  - README.md
autonomous: true
requirements:
  - TECH-01
must_haves:
  truths:
    - "requirements.txt contains no >= version bounds — every line is pinned exactly"
    - "Flask-Limiter appears in requirements.txt at the exact version installed"
    - "README.md reflects the Phase 25 changes (key security + performance notes)"
  artifacts:
    - path: "requirements.txt"
      provides: "Fully pinned dependency list from pip freeze"
      contains: "Flask==3.1.2"
  key_links:
    - from: "requirements.txt"
      to: "venv pip freeze"
      via: "exact version pins captured from running venv — no >= bounds"
---

<objective>
Pin all requirements.txt dependencies to exact versions from pip freeze, ensuring Flask-Limiter (added in plan 02) is included, and update README.md with Phase 25 changes.

Purpose: Prevent surprise breakage on fresh Render deploys caused by floating >= version bounds resolving to incompatible library versions.
Output: requirements.txt with all exact pins from current venv, including Flask-Limiter==4.1.1 and pre-commit. README.md updated with security and performance notes.
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins/25-RESEARCH.md
@.planning/phases/25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins/25-02-SUMMARY.md
@.planning/phases/25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins/25-04-SUMMARY.md

<interfaces>
<!-- TECH-01: Pin requirements.txt -->
<!-- Run: venv/bin/pip freeze > /tmp/current_freeze.txt -->
<!-- This captures ALL installed packages including Flask-Limiter (installed in plan 02) -->
<!-- and pre-commit (installed in plan 03) -->
<!-- Known pinned versions from research (verify against actual freeze output): -->
<!--
beautifulsoup4==4.13.3
Flask==3.1.2
gunicorn==21.2.0
numpy==2.4.4
pandas==3.0.2
plotly==6.1.2
requests==2.32.3
scikit-learn==1.8.0
scipy==1.17.1
torch==2.7.1
transformers==4.53.0
yfinance==0.2.58
cachetools==5.5.2
Flask-Limiter==4.1.1
-->
<!-- Strategy: use pip freeze output directly but FILTER OUT dev-only packages (pre-commit) -->
<!-- pre-commit is a dev tool — do not add it to the production requirements.txt used by Render -->
<!-- Also filter out packages that are only present in venv transitive deps not needed in prod -->
<!-- The CLAUDE.md project instruction requires README.md update with every feature/fix commit -->

<!-- README.md update — keep to 1-3 sentences per change, relevant sections: -->
<!-- Security section (add if absent) or Features/Notes section: -->
<!-- - Added startup guard: server refuses to start without SECRET_KEY environment variable -->
<!-- - Email report route validates recipient against config.json allowlist -->
<!-- - Rate limiting added to analytics routes (regime detection, scrape, Heston calibration) -->
<!-- - API keys no longer accepted in request body — server reads from environment variables only -->
<!-- Performance section: -->
<!-- - HMM regime detection results cached (15-min TTL) to skip repeated model fits -->
<!-- - Bounded LRU/TTL caches replace unbounded dict caches, preventing memory growth -->
<!-- - Gunicorn updated to 2 workers for concurrent request handling on Render -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin requirements.txt from pip freeze</name>
  <files>requirements.txt</files>
  <action>
    Run pip freeze to get the exact current state of the venv:
    ```bash
    venv/bin/pip freeze
    ```

    Inspect the output. Rewrite requirements.txt using the exact pinned versions from this output. Rules:
    1. Replace every `>=` bound with `==` at the exact installed version
    2. Include Flask-Limiter==4.1.1 (installed in plan 02) — verify it appears in freeze output
    3. Do NOT include pre-commit — it is a dev-only tool and not needed on Render's build
    4. Keep all packages that are currently in requirements.txt (just change >= to ==)
    5. Use the `==` format: `PackageName==X.Y.Z` — no spaces around ==
    6. Preserve the existing grouping/ordering/comments in the file if any exist

    After writing requirements.txt, run the test:
    pytest tests/test_unit_codebase_health.py::test_requirements_pinned -x -q

    If the test checks for absence of `>=` patterns, it should now pass.
  </action>
  <verify>
    <automated>pytest tests/test_unit_codebase_health.py::test_requirements_pinned -x -q</automated>
  </verify>
  <done>test_requirements_pinned passes. requirements.txt has no >= bounds. Flask-Limiter==4.1.1 is present. All packages from the original requirements.txt are present with == pins.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md with Phase 25 changes</name>
  <files>README.md</files>
  <action>
    Read README.md and find the appropriate sections to update. Add brief notes (1-3 sentences each) for the Phase 25 changes:

    **Security improvements** (add to Security section or Features section):
    - Server now requires SECRET_KEY environment variable on startup — fails loudly rather than using a hardcoded fallback.
    - Email report endpoint validates recipient addresses against a configurable allowlist in config.json.
    - Rate limiting applied to compute-heavy routes (regime detection, scraping, Heston calibration) — 5 req/min per IP on analytics, 10 req/min on scraping.
    - API keys (Alpha Vantage, Finnhub) are now read exclusively from environment variables; client-side key passing removed.

    **Performance improvements** (add to Performance or Technical Notes section):
    - HMM regime detection results are cached with a 15-minute TTL, eliminating repeated model fitting for the same ticker and date range.
    - All in-memory caches are now bounded (LRU/TTL eviction) to prevent unbounded memory growth.
    - Gunicorn updated to 2 workers to allow concurrent request handling without blocking under Render's 512MB ceiling.

    **Bug fixes** (add to Bug Fixes or Changelog section if it exists):
    - Fixed percentile rank computation returning 50 for all non-exact float matches (now uses bisect for correct ranking).
    - Fixed broken advanced-settings element reference in the JS settings drawer.
    - Removed debug print() calls from production source files; replaced with structured logging.

    Keep updates concise — the CLAUDE.md instruction says 1-3 sentences per change. Do not write a wall of text.
  </action>
  <verify>
    <automated>grep -c "SECRET_KEY\|rate limit\|percentile\|workers 2" README.md</automated>
  </verify>
  <done>README.md updated with security, performance, and bug fix notes for Phase 25. At least 4 of the key Phase 25 terms appear in README.md (SECRET_KEY, rate limit, percentile or bisect, workers).</done>
</task>

</tasks>

<verification>
pytest tests/test_unit_codebase_health.py::test_requirements_pinned -v
grep ">=" requirements.txt | wc -l    # must be 0
grep "Flask-Limiter" requirements.txt  # must show Flask-Limiter==4.1.1
grep "Flask==" requirements.txt        # must show Flask==3.1.2
pytest tests/test_unit_codebase_health.py -v  # full unit suite green
pytest -q --tb=short | tail -5         # full suite green
</verification>

<success_criteria>
- test_requirements_pinned: PASS
- requirements.txt has zero >= bounds
- Flask-Limiter==4.1.1 present in requirements.txt
- All originally declared packages present with exact == pins
- README.md updated with Phase 25 security and performance notes
- Full test suite: 0 failures (all 13 Phase 25 items verified green)
</success_criteria>

<output>
After completion, create `.planning/phases/25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins/25-05-SUMMARY.md`
</output>
