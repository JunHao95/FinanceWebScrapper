---
phase: 25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - webapp.py
  - config.json
  - config.json.example
  - requirements.txt
  - static/js/stockScraper.js
  - templates/index.html
  - .env.example
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-04
user_setup:
  - service: flask-limiter
    why: "Rate limiting on expensive analytics routes"
    env_vars: []
    dashboard_config: []
must_haves:
  truths:
    - "Starting the app without SECRET_KEY set raises RuntimeError — no silent fallback"
    - "POST /api/send_report with an unlisted email address returns HTTP 403"
    - "Sending more than 5 requests/min to /api/regime_detection returns HTTP 429"
    - "POST /api/scrape with alpha_key or finhub_key in request body — those fields are ignored (server reads keys from env only)"
    - "No API key input fields exist in the frontend settings drawer"
  artifacts:
    - path: "webapp.py"
      provides: "SECRET_KEY guard, email allowlist, rate limiter, env-only key reads"
      contains: "RuntimeError"
    - path: "config.json"
      provides: "recipients allowlist for email validation"
      contains: "recipients"
    - path: "requirements.txt"
      provides: "Flask-Limiter pinned entry"
      contains: "Flask-Limiter"
    - path: "templates/index.html"
      provides: "API key form fields removed"
  key_links:
    - from: "webapp.py"
      to: "os.environ"
      via: "SECRET_KEY read with RuntimeError guard on missing"
    - from: "webapp.py send_email_report()"
      to: "config.json recipients list"
      via: "config.get('recipients', []) allowlist check → HTTP 403"
    - from: "webapp.py @limiter.limit"
      to: "/api/regime_detection, /api/scrape, /api/calibrate_heston"
      via: "Flask-Limiter decorator per route"
---

<objective>
Apply all four security patches: add a startup RuntimeError guard for missing SECRET_KEY, add email recipient allowlist validation, install Flask-Limiter and rate-limit expensive routes, and remove all client-side API key passing (server reads keys from env vars only).

Purpose: Close four confirmed security vulnerabilities — hardcoded secret fallback, unvalidated email recipients, unthrottled compute routes, and API keys leaking through request bodies.
Output: App refuses to start without SECRET_KEY, email route enforces allowlist, expensive routes are rate-limited at 5 req/min, client-side API key fields removed from frontend and backend.
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins/25-RESEARCH.md
@.planning/phases/25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins/25-01-SUMMARY.md

<interfaces>
<!-- SEC-01: webapp.py line 51 current code -->
<!-- app.config['SECRET_KEY'] = os.environ.get('SECRET_KEY', 'your-secret-key-here') -->
<!-- Replace with: -->
<!--
_secret_key = os.environ.get('SECRET_KEY')
if not _secret_key:
    raise RuntimeError(
        "SECRET_KEY environment variable is not set. "
        "Generate one with: python -c \"import secrets; print(secrets.token_hex(32))\""
    )
app.config['SECRET_KEY'] = _secret_key
-->

<!-- SEC-02: webapp.py line ~614 current code -->
<!-- recipients = payload.get('email') — no validation -->
<!-- config.json currently has "email": "teejunhao@gmail.com" but NO "recipients" key -->
<!-- Add to config.json: "recipients": ["teejunhao@gmail.com"] -->
<!-- Add to config.json.example: "recipients": [] — with comment that empty disables guard -->
<!-- Route fix: -->
<!--
allowed = config.get('recipients', [])
if allowed and recipients not in allowed:
    return jsonify({'success': False, 'error': 'Recipient not in allowed list'}), 403
-->

<!-- SEC-03: Flask-Limiter 4.1.1 setup -->
<!-- Install: pip install Flask-Limiter==4.1.1 -->
<!-- Add to requirements.txt: Flask-Limiter==4.1.1 -->
<!-- Initialization (after app = Flask(__name__)):
from flask_limiter import Limiter
from flask_limiter.util import get_remote_address
limiter = Limiter(
    app=app,
    key_func=get_remote_address,
    default_limits=["200 per day", "50 per hour"],
    storage_uri="memory://"
)
-->
<!-- Route decorators (5 req/min on heavy analytics, 10 req/min on scrape):
@app.route('/api/regime_detection', methods=['POST'])
@limiter.limit("5 per minute")

@app.route('/api/scrape', methods=['POST'])
@limiter.limit("10 per minute")

Also apply @limiter.limit("5 per minute") to:
  /api/calibrate_heston, /api/calibrate_merton, /api/calibrate_bcc
  /api/stoch_portfolio_mdp, /api/rl_portfolio_rotation_pi, /api/rl_portfolio_rotation_ql
-->

<!-- SEC-04: Remove client-side API key passing -->
<!-- stockScraper.js lines 70–71: remove const alphaKey and const finhubKey reads -->
<!-- stockScraper.js lines 93–94: remove alpha_key and finhub_key from request body object -->
<!-- webapp.py lines 386–387: change
  alpha_key = data.get('alpha_key') or os.environ.get("ALPHA_VANTAGE_API_KEY")
  to:
  alpha_key = os.environ.get("ALPHA_VANTAGE_API_KEY")
  (same for finhub_key)
-->
<!-- templates/index.html lines 1334–1341: remove the two form-group divs for alpha/finhub keys -->
<!-- Remove route docstring mentions of alpha_key/finhub_key body fields -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEC-01 secret key guard + SEC-02 email allowlist</name>
  <files>webapp.py, config.json, config.json.example, .env.example</files>
  <action>
    **SEC-01 — Secret key startup guard:**
    In webapp.py, find the line (around line 51):
      `app.config['SECRET_KEY'] = os.environ.get('SECRET_KEY', 'your-secret-key-here')`
    Replace it with:
    ```python
    _secret_key = os.environ.get('SECRET_KEY')
    if not _secret_key:
        raise RuntimeError(
            "SECRET_KEY environment variable is not set. "
            "Generate one with: python -c \"import secrets; print(secrets.token_hex(32))\""
        )
    app.config['SECRET_KEY'] = _secret_key
    ```
    In .env.example, add a comment below the SECRET_KEY line explaining how to generate it:
      `# SECRET_KEY=<run: python -c "import secrets; print(secrets.token_hex(32))">`

    **SEC-02 — Email allowlist:**
    In config.json, add `"recipients": ["teejunhao@gmail.com"]` as a top-level key alongside existing keys.
    In config.json.example, add `"recipients": []` with a comment: `// empty list disables the allowlist check`.

    In webapp.py, find the send_email_report() route (around line 614). After the line that reads `recipients = payload.get('email')`, add:
    ```python
    allowed = config.get('recipients', [])
    if allowed and recipients not in allowed:
        return jsonify({'success': False, 'error': 'Recipient not in allowed list'}), 403
    ```

    Run: pytest tests/test_unit_codebase_health.py::test_secret_key_guard tests/test_integration_routes.py::test_email_allowlist -x -q
  </action>
  <verify>
    <automated>pytest tests/test_unit_codebase_health.py::test_secret_key_guard tests/test_integration_routes.py::test_email_allowlist -x -q</automated>
  </verify>
  <done>test_secret_key_guard passes (RuntimeError raised when SECRET_KEY unset). test_email_allowlist passes (403 returned for unlisted recipient).</done>
</task>

<task type="auto">
  <name>Task 2: SEC-03 rate limiting + SEC-04 remove client API keys</name>
  <files>webapp.py, requirements.txt, static/js/stockScraper.js, templates/index.html</files>
  <action>
    **SEC-03 — Rate limiting:**
    Install Flask-Limiter: `pip install Flask-Limiter==4.1.1`
    Add `Flask-Limiter==4.1.1` to requirements.txt (exact pin, new line under Flask dependencies section).

    In webapp.py, after the `from flask_cors import CORS` import block (top of file), add:
    ```python
    from flask_limiter import Limiter
    from flask_limiter.util import get_remote_address
    ```

    After `app = Flask(__name__)` and CORS setup (around line 40), add:
    ```python
    limiter = Limiter(
        app=app,
        key_func=get_remote_address,
        default_limits=["200 per day", "50 per hour"],
        storage_uri="memory://"
    )
    ```

    Add `@limiter.limit("5 per minute")` decorator directly under `@app.route(...)` for these routes:
    - /api/regime_detection (POST)
    - /api/calibrate_heston (POST)
    - /api/calibrate_merton (if it exists)
    - /api/calibrate_bcc (POST)
    - /api/stoch_portfolio_mdp (POST) if it exists
    - /api/rl_portfolio_rotation_pi (POST) if it exists
    - /api/rl_portfolio_rotation_ql (POST) if it exists

    Add `@limiter.limit("10 per minute")` to /api/scrape (POST).

    Note: Flask-Limiter 4.x uses `Limiter(app=app, key_func=...)` — NOT the v3.x positional arg order. Use keyword arguments only.

    **SEC-04 — Remove client-side API key passing:**
    In static/js/stockScraper.js:
    - Remove the lines that read `const alphaKey = ...` and `const finhubKey = ...` (around lines 70–71)
    - In the fetch body object (around lines 93–94), remove `alpha_key: alphaKey || undefined` and `finhub_key: finhubKey || undefined`

    In webapp.py, find the /api/scrape handler. Change:
    ```python
    alpha_key = data.get('alpha_key') or os.environ.get("ALPHA_VANTAGE_API_KEY")
    finhub_key = data.get('finhub_key') or os.environ.get("FINHUB_API_KEY")  # or FINNHUB_API_KEY
    ```
    to:
    ```python
    alpha_key = os.environ.get("ALPHA_VANTAGE_API_KEY")
    finhub_key = os.environ.get("FINHUB_API_KEY")
    ```

    In templates/index.html, locate and remove the two `<div class="form-group">` blocks that contain the alpha_key and finhub_key input fields (around lines 1334–1341). Remove the entire div block for each.

    Remove any route docstring or comment in webapp.py that mentions alpha_key/finhub_key as accepted body fields.

    Run tests:
    pytest tests/test_integration_routes.py::test_rate_limiting tests/test_integration_routes.py::test_no_client_api_keys -x -q
  </action>
  <verify>
    <automated>pytest tests/test_integration_routes.py::test_rate_limiting tests/test_integration_routes.py::test_no_client_api_keys tests/test_unit_codebase_health.py -x -q</automated>
  </verify>
  <done>test_rate_limiting passes (429 returned after rate limit exceeded). test_no_client_api_keys passes (scrape route ignores alpha_key in body). No API key input fields in index.html. All 9 unit tests in test_unit_codebase_health.py that are now fixable pass (test_secret_key_guard, test_cache_bounded skipped until plan 04).</done>
</task>

</tasks>

<verification>
pytest tests/test_unit_codebase_health.py::test_secret_key_guard tests/test_integration_routes.py::test_email_allowlist tests/test_integration_routes.py::test_rate_limiting tests/test_integration_routes.py::test_no_client_api_keys -v
grep "RuntimeError" webapp.py              # SECRET_KEY guard
grep "recipients" config.json              # allowlist present
grep "Flask-Limiter" requirements.txt      # pinned
grep "limiter.limit" webapp.py | wc -l    # should be >= 7 decorated routes
grep "alpha_key" static/js/stockScraper.js # should return nothing
grep "alphaKey" static/js/stockScraper.js  # should return nothing
grep "alpha_key\|finhub_key" templates/index.html  # should return nothing
</verification>

<success_criteria>
- test_secret_key_guard: PASS
- test_email_allowlist: PASS (403 for unlisted recipient)
- test_rate_limiting: PASS (429 after N+1 requests)
- test_no_client_api_keys: PASS
- Flask-Limiter 4.1.1 installed and in requirements.txt
- At least 7 routes decorated with @limiter.limit
- No alpha_key / finhub_key in frontend JS or HTML
- app.config['SECRET_KEY'] uses env var only with RuntimeError guard
</success_criteria>

<output>
After completion, create `.planning/phases/25-codebase-health-critical-bug-fixes-security-patches-and-performance-quick-wins/25-02-SUMMARY.md`
</output>
