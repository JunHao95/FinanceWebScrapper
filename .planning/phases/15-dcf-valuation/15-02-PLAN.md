---
phase: 15-dcf-valuation
plan: "02"
type: execute
wave: 2
depends_on: ["15-01"]
files_modified: []
autonomous: false
requirements: [DCF-01, DCF-02, DCF-03, DCF-04, DCF-05]

must_haves:
  truths:
    - "A ticker with FCF data shows a dollar intrinsic value in the Deep Analysis section"
    - "The premium/discount badge is colour-coded (green for discount, red for premium)"
    - "WACC, Stage 1 growth, Stage 2 growth inputs are visible with defaults 10, 10, 3"
    - "Changing WACC to 12 and clicking Recalculate updates the value without page reload"
    - "A ticker with no FCF data shows 'DCF unavailable — FCF data missing' with no numeric output"
  artifacts:
    - path: "static/js/dcfValuation.js"
      provides: "Running module confirmed by browser render"
  key_links:
    - from: "browser Deep Analysis section"
      to: "dcfValuation.js renderIntoGroup"
      via: "displayManager.createTickerCard trigger"
      pattern: "dcf-section visible in DOM"
---

<objective>
Human verification checkpoint: confirm all five DCF success criteria are satisfied in a live browser session before Phase 15 is declared complete.

Purpose: The module is pure client-side with no automated browser test — human eyes confirm rendering, badge colours, recalculate behaviour, and the FCF-absent degradation path.
Output: No file changes. Checkpoint approval unblocks Phase 16.
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-dcf-valuation/15-CONTEXT.md
@.planning/phases/15-dcf-valuation/15-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: DCF Valuation end-to-end in browser</name>
  <files>none — verification only, no file changes</files>
  <action>
Start the Flask dev server (`python webapp.py`) and run the five checks below in a browser at http://127.0.0.1:5000.

**Check 1 — Intrinsic value renders (DCF-01):**
1. Enter a ticker with Alpha Vantage FCF data (e.g., AAPL with an AV key configured) and click Run Analysis.
2. After results load, navigate to the Stocks tab and expand the AAPL ticker card.
3. Scroll to the bottom of the card and expand the Deep Analysis section.
4. Confirm a DCF subsection appears showing "💰 DCF Value: $XX.XX" with a dollar figure per share.
5. Confirm a source footnote is shown (e.g., "FCF source: Alpha Vantage").

**Check 2 — Premium/discount badge (DCF-02):**
1. Confirm a signed percentage badge appears alongside the intrinsic value.
2. If the current price exceeds intrinsic value, badge reads "+XX.X% premium" in red.
3. If the current price is below intrinsic value, badge reads "−XX.X% discount" in green.
4. Confirm the colour is correct (badge-danger = red for premium; badge-success = green for discount).

**Check 3 — Assumptions visible (DCF-03):**
1. With the DCF section expanded, confirm three input fields are visible: WACC, Stage 1 growth, Stage 2 growth.
2. Confirm their default values are 10, 10, and 3 respectively.
3. Confirm a "Recalculate" button is present.

**Check 4 — Recalculate without re-scrape (DCF-04):**
1. Change the WACC input from 10 to 12.
2. Click the "Recalculate" button.
3. Confirm the intrinsic value and premium/discount percentage update in-place.
4. Confirm the page did NOT reload (other ticker results and the tab state are unchanged).
5. Change WACC back to 10 and recalculate again — confirm value returns close to original.

**Check 5 — FCF absent degradation (DCF-05):**
1. Scrape a ticker known to have no FCF data (try a small-cap or OTC ticker, or remove the AV API key temporarily).
2. Confirm the DCF section displays "DCF unavailable — FCF data missing" with no dollar figure, no badge, and no inputs.
3. Open DevTools Console and confirm no JavaScript errors are thrown.
  </action>
  <verify>
    <automated>grep -n "DCFValuation.renderIntoGroup" static/js/displayManager.js && grep -n "dcfValuation" static/js/stockScraper.js && grep -n "dcfValuation.js" templates/index.html</automated>
  </verify>
  <done>Human confirms all five checks pass. Phase 15 is complete and ready for Phase 16 planning.</done>
  <resume-signal>Type "approved" if all five checks pass. Describe any failing check with observed vs expected behaviour if issues are found.</resume-signal>
</task>

</tasks>

<verification>
All five browser checks pass: intrinsic value renders, badge colour is correct, assumption inputs visible with correct defaults, recalculate updates in-place, FCF-absent case degrades gracefully with no console errors.
</verification>

<success_criteria>
Human approves all five checks. Phase 15 is complete.
</success_criteria>

<output>
After approval, create `.planning/phases/15-dcf-valuation/15-02-SUMMARY.md`
</output>
