---
phase: 22-liquidity-sweep-composite-bias-tab-wiring
plan: 03
type: execute
wave: 3
depends_on:
  - "22-01"
  - "22-02"
files_modified: []
autonomous: false
requirements:
  - SWEEP-01
  - SWEEP-02
  - SWEEP-03
  - BIAS-01
  - BIAS-02
  - BIAS-03
  - TIND-01
  - TIND-02
  - TIND-03

must_haves:
  truths:
    - "Trading Indicators tab is visible in the results tab bar and renders the 2x2 grid without re-scraping"
    - "Each ticker card shows a colored composite bias badge and caveat line"
    - "The Liquidity Sweep panel (bottom-right) shows a candlestick chart with sweep markers or a grey placeholder"
    - "Changing lookback from 90d to 30d clears all cards and re-renders with new data"
    - "No JavaScript console errors when navigating the tab or changing the lookback"
  artifacts:
    - path: "static/js/tradingIndicators.js"
      provides: "complete implementation verified visually"
    - path: "src/analytics/trading_indicators.py"
      provides: "compute_liquidity_sweep and compute_composite_bias verified via test suite"
  key_links:
    - from: "Browser tab click"
      to: "2x2 grid rendered per ticker"
      via: "tabs.js lazy-load → fetchForTicker → _renderTickerCard"
    - from: "Lookback dropdown change"
      to: "all cards cleared and re-fetched"
      via: "_initLookbackDropdown change event"
---

<objective>
Visual and functional checkpoint: confirm all nine Phase 22 requirements are satisfied in the live browser with real ticker data before marking the phase complete.

Purpose: Phase 22 is the final v2.2 feature phase — full tab wiring must be verified against the live app before Phase 23 test suite work begins.
Output: User confirms go/no-go; any issues are documented for targeted fixes.
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/22-liquidity-sweep-composite-bias-tab-wiring/22-01-SUMMARY.md
@.planning/phases/22-liquidity-sweep-composite-bias-tab-wiring/22-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run test suite to confirm backend correctness</name>
  <files></files>
  <action>
Run the full trading indicators test suite to confirm all Phase 22 backend work is green before the visual checkpoint:

```bash
pytest tests/test_trading_indicators.py -v
```

Expected: all tests pass including TestComputeLiquiditySweep and TestComputeCompositeBias.

If any test fails, fix the issue in `src/analytics/trading_indicators.py` or `webapp.py` before proceeding to the human-verify checkpoint.
  </action>
  <verify>
    <automated>pytest tests/test_trading_indicators.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All tests in tests/test_trading_indicators.py pass with exit code 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 22 implementation:
    - Liquidity Sweep Python backend (adaptive n, look-ahead-safe detection, candlestick chart with markers + dashed lines)
    - Composite Bias Python backend (majority vote, dissenter identification, unavailable handling)
    - /api/trading_indicators route updated to return real sweep + bias data
    - 2×2 CSS grid layout per ticker card in Trading Indicators tab
    - Composite bias badge (colored dot + score + dissenter name) above grid
    - Lookback dropdown (30/90/180/365d) in tab header with cache-clear + progressive re-fetch
    - All four Plotly panels use staticPlot: true
  </what-built>
  <how-to-verify>
    **Setup:** Start the Flask dev server (`python webapp.py`) and open http://localhost:5000 in your browser. Open DevTools Console (F12).

    **Step 1 — Scrape a ticker:**
    Enter `AAPL` in the stock scraper and click Run Analysis. Wait for results to appear.

    **Step 2 — Open Trading Indicators tab:**
    Click the "Trading Indicators" tab button in the results tab bar.
    Confirm: The tab activates without a page reload. A lookback bar appears at the top showing "Trading Indicators" label and a dropdown defaulted to "90d".

    **Step 3 — Verify 2×2 grid:**
    Check the AAPL ticker card:
    - Top-left: Volume Profile (horizontal histogram with POC/VAH/VAL)
    - Top-right: Anchored VWAP (price chart with 2–3 VWAP lines)
    - Bottom-left: Order Flow (delta bar chart)
    - Bottom-right: Liquidity Sweep (candlestick chart OR grey "Liquidity Sweep unavailable" placeholder)

    **Step 4 — Verify composite bias badge:**
    Above the 2×2 grid, check that a colored dot + text appears, e.g.:
    `● Bullish (3/4) — Order Flow dissents`
    Below it: `Trend-following bias — all indicators share the same OHLCV data source.`

    **Step 5 — Verify Sweep panel:**
    If the Sweep panel shows a chart:
    - Confirm it is a candlestick (green/red bars with wicks)
    - Look for ▲ or ▼ annotations on any candles
    - Look for dashed horizontal lines at swept price levels
    - Badge below the chart should show sweep signal text

    If sweep shows a grey placeholder: confirm the badge text reads "No confirmed swings in selected window (n=X)" or "No Sweep in selected window (n=X)".

    **Step 6 — Verify lookback change:**
    Change the dropdown from 90d to 30d.
    Confirm:
    - All existing ticker cards disappear
    - "Loading AAPL…" placeholder appears briefly
    - Cards re-render with the new 30d data
    - No duplicate cards

    Change back to 90d and confirm the same progressive re-render.

    **Step 7 — Console errors:**
    Check DevTools Console. Confirm: zero red errors. Yellow warnings about missing data are acceptable.

    **Step 8 — Multiple tickers (optional but ideal):**
    Add a second ticker (e.g. `SPY`) and run analysis. Navigate to Trading Indicators. Confirm both AAPL and SPY cards render in the 2×2 grid layout.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all steps pass.
    Or describe any issues found (e.g. "Sweep chart blank for AAPL on 30d" or "Console error: X") so targeted fixes can be made.
  </resume-signal>
</task>

</tasks>

<verification>
Backend: `pytest tests/test_trading_indicators.py -v` — all tests pass.
Frontend: Human visual confirmation of all 5 success criteria below.
</verification>

<success_criteria>
1. Trading Indicators tab renders 2×2 Plotly grid per ticker with VP/AVWAP/OF/Sweep in the correct quadrants
2. Composite bias badge shows colored dot, score (e.g. "3/4"), and dissenter name above the grid
3. Lookback dropdown change clears session + re-renders all tickers progressively
4. Liquidity Sweep panel shows candlestick chart with markers/lines OR grey unavailable placeholder
5. Zero JavaScript console errors when navigating the tab or changing the lookback
</success_criteria>

<output>
After completion, create `.planning/phases/22-liquidity-sweep-composite-bias-tab-wiring/22-03-SUMMARY.md`
</output>
