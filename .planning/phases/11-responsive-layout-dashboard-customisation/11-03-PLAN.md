---
phase: 11-responsive-layout-dashboard-customisation
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01-PLAN.md
files_modified:
  - static/js/tabs.js
  - static/js/peerComparison.js
  - static/js/optionsDisplay.js
  - README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Switching tabs with TabManager.switchTab or switchMainTab scrolls the active tab button into view on mobile"
    - "Peer comparison raw table is wrapped in .table-scroll-wrap div so it scrolls horizontally at 360px"
    - "Options convergence table (optionsDisplay.js) is wrapped in .table-scroll-wrap div"
    - "README.md reflects the Phase 11 responsive and dashboard-customisation changes"
  artifacts:
    - path: "static/js/tabs.js"
      provides: "scrollIntoView calls in switchTab and switchMainTab"
      contains: "scrollIntoView"
    - path: "static/js/peerComparison.js"
      provides: "peer-raw-table wrapped in .table-scroll-wrap"
      contains: "table-scroll-wrap"
    - path: "static/js/optionsDisplay.js"
      provides: "convergence table wrapped in .table-scroll-wrap"
      contains: "table-scroll-wrap"
    - path: "README.md"
      provides: "Phase 11 feature documentation"
      contains: "responsive"
  key_links:
    - from: "static/js/tabs.js switchTab / switchMainTab"
      to: "active tab button DOM element"
      via: "element.scrollIntoView({ behavior: 'smooth', inline: 'nearest', block: 'nearest' })"
      pattern: "scrollIntoView"
    - from: "static/js/peerComparison.js rawTable"
      to: "static/css/styles.css .table-scroll-wrap"
      via: "CSS class applied to wrapper div around <table>"
      pattern: "table-scroll-wrap"
---

<objective>
Add `scrollIntoView` calls to both tab-switching functions in `tabs.js` so the active tab button stays visible during horizontal scrolling on mobile. Wrap multi-column HTML tables in `peerComparison.js` and `optionsDisplay.js` with the `.table-scroll-wrap` div class. Update README.md to document Phase 11 additions.

Purpose: Completes the mobile tab navigation UX and the table overflow handling. Runs in parallel with Plan 02 (no shared files).
Output: tabs.js, peerComparison.js, optionsDisplay.js, README.md updated.
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-responsive-layout-dashboard-customisation/11-CONTEXT.md
@.planning/phases/11-responsive-layout-dashboard-customisation/11-RESEARCH.md

<interfaces>
<!-- Key function signatures and table HTML patterns that the executor will modify -->

From static/js/tabs.js — switchTab (lines 9–73):
```javascript
switchTab(tabName) {
    // ...
    if (tabName === 'stocks') {
        const stocksTab = document.getElementById('stocksTab');
        const stocksContent = document.getElementById('stocksTabContent');
        if (stocksTab && stocksContent) {
            stocksTab.classList.add('active');
            stocksContent.classList.add('active');
            // <-- ADD scrollIntoView HERE after classList.add('active')
        }
    } else if (tabName === 'analytics') {
        const analyticsTab = document.getElementById('analyticsTab');
        // ...
            analyticsTab.classList.add('active');
            // <-- ADD scrollIntoView HERE
    } else if (tabName === 'autoanalysis') {
        const autoanalysisTab = document.getElementById('autoanalysisTab');
        // ...
            autoanalysisTab.classList.add('active');
            // <-- ADD scrollIntoView HERE
    } else if (tabName === 'tradingindicators') {
        const tiTab = document.getElementById('tradingIndicatorsTab');
        // ...
            tiTab.classList.add('active');
            // <-- ADD scrollIntoView HERE
    }
}
```

From static/js/tabs.js — switchMainTab (lines 78–117):
```javascript
switchMainTab(tabName, event) {
    // ...
    const targetButton = event
        ? (event.target.closest('.main-tab-button') || event.currentTarget)
        : document.querySelector(`.main-tab-button[onclick*="${tabName}"]`);
    if (targetButton) {
        targetButton.classList.add('active');
        // <-- ADD scrollIntoView HERE after classList.add('active')
    }
}
```

From static/js/peerComparison.js (lines 114–123):
```javascript
var rawTable = '<table class="peer-raw-table" style="display:none;width:100%;border-collapse:collapse;margin-top:8px;font-size:12px;">'
    + '<thead>...</thead>'
    + '<tbody>' + tableRows + '</tbody>'
    + '</table>';
// rawTable is embedded in sectionEl innerHTML — wrap it:
// CHANGE TO: '<div class="table-scroll-wrap">' + rawTable + '</div>'
```

From static/js/optionsDisplay.js (lines 167–181):
```javascript
html += '<table style="width: 100%; border-collapse: collapse;">';
html += '<tr><th>Iter</th><th>Sigma</th><th>Price</th><th>Diff</th></tr>';
// ... rows ...
html += '</table>';
// CHANGE TO: wrap the table open and close with .table-scroll-wrap div
```

CSS class added by Plan 01 (must exist):
- `.table-scroll-wrap { width: 100%; overflow-x: auto; -webkit-overflow-scrolling: touch; }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scrollIntoView to tabs.js switchTab and switchMainTab</name>
  <files>static/js/tabs.js</files>
  <action>
In `switchTab`, after every `classList.add('active')` call on a TAB BUTTON element (not the content div), add:
```javascript
tabButton.scrollIntoView({ behavior: 'smooth', inline: 'nearest', block: 'nearest' });
```

Replace the variable name with the actual button variable in each branch:
- `stocksTab` branch: after `stocksTab.classList.add('active')` → `stocksTab.scrollIntoView({ behavior: 'smooth', inline: 'nearest', block: 'nearest' });`
- `analyticsTab` branch: after `analyticsTab.classList.add('active')` → `analyticsTab.scrollIntoView({ behavior: 'smooth', inline: 'nearest', block: 'nearest' });`
- `autoanalysisTab` branch: after `autoanalysisTab.classList.add('active')` → `autoanalysisTab.scrollIntoView({ behavior: 'smooth', inline: 'nearest', block: 'nearest' });`
- `tradingIndicatorsTab` (tiTab) branch: after `tiTab.classList.add('active')` → `tiTab.scrollIntoView({ behavior: 'smooth', inline: 'nearest', block: 'nearest' });`

In `switchMainTab`, after `targetButton.classList.add('active')` (inside the `if (targetButton)` block), add:
```javascript
targetButton.scrollIntoView({ behavior: 'smooth', inline: 'nearest', block: 'nearest' });
```

The `scrollIntoView` call must be INSIDE the same `if` guard that checks the element exists — do NOT call it unconditionally.
  </action>
  <verify>
    <automated>grep -c "scrollIntoView" /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper/static/js/tabs.js</automated>
  </verify>
  <done>
  - At least 5 `scrollIntoView` calls present in tabs.js (one per tab branch in switchTab + one in switchMainTab)
  - Each call uses `{ behavior: 'smooth', inline: 'nearest', block: 'nearest' }`
  - Each call is guarded by the element existence check (inside existing `if` blocks)
  - `pytest tests/ -q` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Wrap tables in table-scroll-wrap, update README.md</name>
  <files>static/js/peerComparison.js, static/js/optionsDisplay.js, README.md</files>
  <action>
**A. peerComparison.js — wrap peer-raw-table:**

Find the line that assigns `rawTable`:
```javascript
var rawTable = '<table class="peer-raw-table" style="display:none;width:100%;...">...'
```

Wrap the entire `rawTable` string in a `.table-scroll-wrap` div. The `display:none` must stay on the TABLE element (not on the wrapper div) so the show/toggle logic in `toggleRawPeers` still works. Change to:
```javascript
var rawTable = '<div class="table-scroll-wrap"><table class="peer-raw-table" style="display:none;width:100%;border-collapse:collapse;margin-top:8px;font-size:12px;">'
    + '<thead>...</thead>'
    + '<tbody>' + tableRows + '</tbody>'
    + '</table></div>';
```

Verify the existing `toggleRawPeers` function references `.peer-raw-table` via `sectionEl.querySelector('.peer-raw-table')` — this will still work because querySelector searches descendants. No change needed to `toggleRawPeers`.

Also check whether there are any other `<table>` elements in analyticsRenderer.js or displayManager.js that should receive the wrapper. Read lines producing `<table>` in displayManager.js and only wrap tables with more than 2 columns (multi-column overflow risk). Add `table-scroll-wrap` wrapper to any multi-column table found in analyticsRenderer.js inline HTML strings.

---

**B. optionsDisplay.js — wrap convergence table:**

Find the section that builds the convergence details table (around line 167):
```javascript
html += '<table style="width: 100%; border-collapse: collapse;">';
html += '<tr><th>Iter</th><th>Sigma</th><th>Price</th><th>Diff</th></tr>';
// ... row loop ...
html += '</table>';
```

Change to:
```javascript
html += '<div class="table-scroll-wrap"><table style="width: 100%; border-collapse: collapse;">';
html += '<tr><th>Iter</th><th>Sigma</th><th>Price</th><th>Diff</th></tr>';
// ... row loop unchanged ...
html += '</table></div>';
```

---

**C. README.md — add Phase 11 feature note:**

Read README.md to find the Features section (or a similar top-level section). Add a concise entry documenting Phase 11:

Under a "Features" or "Recent Changes" section, add:
```
**Responsive Layout & Dashboard Customisation (Phase 11):** The app is now fully functional at 360px–1024px+ screen widths. Fixed-width overflow culprits (chatbot window, settings drawer, allocation rows) are corrected via CSS media queries. Tab navigation scrolls horizontally on mobile without a visible scrollbar. Per-ticker card sections (Financial Health Score, Regime Detection, Portfolio Health) can be collapsed and expanded with a chevron toggle; collapsed state persists within the browser session.
```

Keep the addition to 3 sentences or fewer.
  </action>
  <verify>
    <automated>grep -c "table-scroll-wrap" /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper/static/js/peerComparison.js && grep -c "table-scroll-wrap" /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper/static/js/optionsDisplay.js && grep -c "responsive\|Phase 11" /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper/README.md</automated>
  </verify>
  <done>
  - `peerComparison.js`: `.peer-raw-table` is wrapped in a `div.table-scroll-wrap` — `toggleRawPeers` still works (table still has `display:none`, wrapper is always visible)
  - `optionsDisplay.js`: convergence `<table>` is wrapped in `div.table-scroll-wrap`
  - `README.md`: Phase 11 feature mentioned (responsive, collapse toggles)
  - `pytest tests/ -q` passes (no backend regressions)
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/ -q` passes — no regressions
2. `grep -c "scrollIntoView" static/js/tabs.js` returns 5+
3. Browser at 480px DevTools: click Analytics tab → tab button scrolls into view without page jump
4. Browser at 360px: open Peer Comparison → expand raw table → table scrolls horizontally inside its container
5. Browser at 360px: open Options Pricing → run calculation → convergence table scrolls horizontally
</verification>

<success_criteria>
- `scrollIntoView` present in all 4 tab branches of `switchTab` + 1 in `switchMainTab`
- `.table-scroll-wrap` div wraps `peer-raw-table` in peerComparison.js
- `.table-scroll-wrap` div wraps convergence table in optionsDisplay.js
- README.md updated with Phase 11 description
- `pytest tests/ -q` passes
</success_criteria>

<output>
After completion, create `.planning/phases/11-responsive-layout-dashboard-customisation/11-03-SUMMARY.md`
</output>
