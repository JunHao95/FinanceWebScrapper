---
phase: 10-chatbot-integration
plan: 02
type: execute
wave: 2                     # Execution wave (1, 2, 3...)
depends_on: ["10-01"]              # Plan IDs this plan requires
files_modified: ["webapp.py", "requirements.txt"]          # Files this plan touches
autonomous: true            # false if plan has checkpoints
requirements: ["CHAT-01"]            # REQUIRED — Requirement IDs from ROADMAP this plan addresses. MUST NOT be empty.

must_haves:
  truths:
    - "Chatbot uses an LLM (OpenAI or local fallback) to generate suitable answers to financial questions"
    - "Backend parses and manages memory or prompts strictly via the LLM API"
  artifacts:
    - path: "webapp.py"
      provides: "Updated /api/chat route talking to an LLM provider instead of using a placeholder string"
  key_links:
    - from: "webapp.py"
      to: "LLM API provider (e.g. OpenAI SDK)"
      via: "Integration logic"
---

<objective>
Upgrade the QuantAssistant backend endpoint in `webapp.py` to route user queries through an LLM to generate intelligent, context-aware responses rather than the static fallback string. 
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add openai dependency</name>
  <files>requirements.txt</files>
  <action>
    Append `openai` to the end of `requirements.txt` to ensure the project has the necessary library to instantiate LLM models.
  </action>
  <verify>
    <automated>grep -i "openai" requirements.txt && echo "Passed"</automated>
  </verify>
  <done>openai is in requirements.txt.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade /api/chat route</name>
  <files>webapp.py</files>
  <action>Open `webapp.py`. Modify the function `chat()` bound to `/api/chat`.
1. Update it to attempt connecting to the `openai` Python SDK (using API keys if present in env `os.environ.get('OPENAI_API_KEY')`).
2. Add a fallback mechanism if `OPENAI_API_KEY` is not present, so the server throws a warning string like "The LLM is currently unplugged - missing API key." 
3. Instruct the system prompt internally within the request to act as an expert MFE (Master of Financial Engineering) quantitative assistant named "QuantAssistant".
4. Read the `message` input, send it to the LLM backend (e.g., `gpt-3.5-turbo` or `gpt-4o-mini`), extract the AI response, and return it inside the `reply` JSON key.
  </action>
  <verify>
    <automated>cat webapp.py | grep -i "openai.ChatCompletion" || cat webapp.py | grep -i "client.chat.completions.create"</automated>
  </verify>
  <done>Returns a dynamically generated smart reply via LLM.</done>
</task>

</tasks>

<verification>
Start the app and attempt to hit the API route simulating an LLM response. (Or verify the missing key fallback works gracefully).
</verification>

<success_criteria>
Chatbot logic delegates intelligent QA responses directly to OpenAI or an equivalent backend implementation, dropping the static strings.
</success_criteria>

<output>
After completion, create `.planning/phases/10-chatbot-integration/10-02-SUMMARY.md`
</output>