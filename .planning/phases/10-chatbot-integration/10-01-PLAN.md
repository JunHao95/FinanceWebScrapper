---
phase: 10-chatbot-integration
plan: 01
type: execute
wave: 1                     # Execution wave (1, 2, 3...)
depends_on: []              # Plan IDs this plan requires
files_modified: ["webapp.py", "templates/index.html", "static/css/styles.css", "static/js/chatbot.js"]          # Files this plan touches
autonomous: true            # false if plan has checkpoints
requirements: ["CHAT-01"]            # REQUIRED — Requirement IDs from ROADMAP this plan addresses. MUST NOT be empty.

must_haves:
  truths:
    - "User can see a floating chat widget"
    - "User can type and send messages to the chatbot"
    - "Chatbot responds as QuantAssistant"
  artifacts:
    - path: "static/js/chatbot.js"
      provides: "Frontend chatbot logic and UI interactions"
    - path: "webapp.py"
      provides: "Backend /api/chat route"
  key_links:
    - from: "static/js/chatbot.js"
      to: "/api/chat"
      via: "fetch API POST request"
      pattern: "fetch\\(./api/chat."
---

<objective>
Integrate QuantAssistant chatbot to interact with users directly from the web application.

Purpose: Provide an intelligent interactive interface (QuantAssistant) for users utilizing the FinanceWebScrapper.
Output: Backend API for chat capability, frontend floating UI, and JS to coordinate conversations.
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Backend Chat Endpoint</name>
  <files>webapp.py</files>
  <action>Open `webapp.py` and register a new route `POST /api/chat`. The endpoint should extract `message` from the JSON payload. For this initial integration, securely parse the data and return a JSON skeleton response: `{"reply": "Hello, I am QuantAssistant. I received your message: " + message}`. Also ensure you import any necessary modules if not present (e.g. `request`, `jsonify` are likely already in Flask).</action>
  <verify>
    <automated>python3 -c "import ast; code=open('webapp.py').read(); print('YES' if any(n.name=='chat' or any(isinstance(d, ast.Call) and getattr(d.func, 'attr', '')=='route' and '/api/chat' in [a.value for a in d.args if isinstance(a, ast.Constant)] for d in n.decorator_list) for n in ast.parse(code).body if isinstance(n, ast.FunctionDef)) else 'NO')"</automated>
  </verify>
  <done>The backend successfully accepts chat messages and sends placeholder replies.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Frontend Chat UI & Wiring</name>
  <files>templates/index.html, static/css/styles.css, static/js/chatbot.js</files>
  <action>
1. Create `static/js/chatbot.js` with logic to build or manage a collapsible chat overlay (a button to open it, a message list, text input, and send button). Use `fetch()` to POST to `/api/chat` and append the response.
2. In `templates/index.html`, add `<script src="/static/js/chatbot.js"></script>` to the bottom scripts section. Add a small HTML block for the chatbot window/button if necessary (or generate entirely via JS).
3. In `static/css/styles.css`, append styles for the floating chat window (bottom right, fixed position, hidden when collapsed).
  </action>
  <verify>
    <automated>grep -q "chatbot.js" templates/index.html && echo "Passed"</automated>
  </verify>
  <done>Chatbot widget is visible, openable, and communicates with `/api/chat`.</done>
</task>

</tasks>

<verification>
Check that `webapp.py` runs cleanly. Check that `/api/chat` can be POSTed to via curl or similar tool.
</verification>

<success_criteria>
Chatbot is fully integrated into the client app and users can get a response from QuantAssistant.
</success_criteria>

<output>
After completion, create `.planning/phases/10-chatbot-integration/10-01-SUMMARY.md`
</output>