---
phase: 31-integration-with-googlesheets-for-local-webapp
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_unit_sheets_utils.py
  - tests/test_integration_routes.py
autonomous: true
requirements: [SHEETS-UNIT, SHEETS-INT]
must_haves:
  truths:
    - "pytest tests/test_unit_sheets_utils.py -x -q runs without import errors (all tests xfail or skip)"
    - "pytest tests/test_integration_routes.py::TestExportSheets -x -q runs without import errors"
    - "Every automated verify in plans 31-02 and 31-03 has a corresponding test stub here"
  artifacts:
    - path: "tests/test_unit_sheets_utils.py"
      provides: "Unit test stubs for serialize_value, get_sheets_client, export_tickers_to_sheets"
      contains: "test_serialize_none, test_serialize_number, test_serialize_bool, test_missing_creds_env, test_missing_creds_file, test_batch_append, test_empty_tickers"
    - path: "tests/test_integration_routes.py"
      provides: "Integration test class TestExportSheets covering POST /api/export-sheets"
      contains: "TestExportSheets"
  key_links:
    - from: "tests/test_unit_sheets_utils.py"
      to: "src/utils/sheets_utils.py"
      via: "import"
      pattern: "from src.utils.sheets_utils import"
    - from: "tests/test_integration_routes.py"
      to: "POST /api/export-sheets"
      via: "client.post('/api/export-sheets', ...)"
      pattern: "client.post.*export-sheets"
---

<objective>
Write failing test stubs for all sheets_utils unit tests and the export-sheets integration tests. These stubs must exist before Wave 1 implementation code is written — this is the TDD Wave 0 requirement. Tests are written to describe expected behavior, not to pass yet.

Purpose: Establish verifiable acceptance criteria before code exists. Every Wave 1 and Wave 2 automated verify command references tests created here.
Output: tests/test_unit_sheets_utils.py (new file) + TestExportSheets class appended to tests/test_integration_routes.py
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-integration-with-googlesheets-for-local-webapp/31-CONTEXT.md
@.planning/phases/31-integration-with-googlesheets-for-local-webapp/31-RESEARCH.md
@.planning/phases/31-integration-with-googlesheets-for-local-webapp/31-VALIDATION.md
@tests/conftest.py
@tests/test_integration_routes.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/test_unit_sheets_utils.py with all unit test stubs</name>
  <files>tests/test_unit_sheets_utils.py</files>
  <behavior>
    - test_serialize_none: serialize_value(None) == ""
    - test_serialize_number_int: serialize_value(42) == 42 (int, not string)
    - test_serialize_number_float: serialize_value(3.14) == 3.14 (float, not string)
    - test_serialize_bool_true: serialize_value(True) == "True" (string, not 1)
    - test_serialize_bool_false: serialize_value(False) == "False" (string, not 0)
    - test_serialize_string: serialize_value("hello") == "hello"
    - test_missing_creds_env: get_sheets_client() raises FileNotFoundError when GOOGLE_SHEETS_CREDENTIALS_PATH not set (env var absent)
    - test_missing_creds_file: get_sheets_client() raises FileNotFoundError when env var set but file does not exist at that path
    - test_batch_append: export_tickers_to_sheets(["AAPL", "MSFT"], {...}) calls worksheet.append_rows() exactly once (batch, not per-row loop) and returns 2
    - test_empty_tickers: export_tickers_to_sheets([], {}) returns 0 and does NOT call worksheet.append_rows()
    - test_row_length: each row produced by export has exactly 20 elements
    - test_none_in_row: a ticker with RSI=None produces "" in the RSI column position (column index 7 in the 20-col schema)
  </behavior>
  <action>
    Create tests/test_unit_sheets_utils.py from scratch. Use pytest.mark.unit on all tests.

    File structure:
    1. Module docstring explaining this tests src/utils/sheets_utils.py
    2. Imports: pytest, unittest.mock (patch, MagicMock), os

    For serialize_value tests (no mocking needed — pure function):
    - Import serialize_value directly: `from src.utils.sheets_utils import serialize_value`
    - Write one test per behavior case listed above (test_serialize_none through test_serialize_string)

    For get_sheets_client tests:
    - Import get_sheets_client: `from src.utils.sheets_utils import get_sheets_client`
    - test_missing_creds_env: use patch.dict("os.environ", {}, clear=True) to remove GOOGLE_SHEETS_CREDENTIALS_PATH, assert raises FileNotFoundError
    - test_missing_creds_file: patch.dict sets path to "/nonexistent/path/creds.json", patch("os.path.exists", return_value=False), assert raises FileNotFoundError

    For export_tickers_to_sheets tests (mock gspread entirely):
    - Import: `from src.utils.sheets_utils import export_tickers_to_sheets`
    - Mock pattern (from RESEARCH.md):
      ```python
      mock_worksheet = MagicMock()
      mock_sh = MagicMock()
      mock_sh.get_worksheet.return_value = mock_worksheet
      mock_gc = MagicMock()
      mock_gc.open_by_key.return_value = mock_sh
      ```
    - Patch: gspread.service_account → mock_gc, os.environ GOOGLE_SHEETS_CREDENTIALS_PATH → "/fake/creds.json", GOOGLE_SHEETS_SPREADSHEET_ID → "fake-id", os.path.exists → True

    test_batch_append: after calling export_tickers_to_sheets(["AAPL", "MSFT"], {"AAPL": {"Price": "175.00"}, "MSFT": {"RSI": None}}), assert mock_worksheet.append_rows.call_count == 1 and return value == 2

    test_empty_tickers: export_tickers_to_sheets([], {}), assert return value == 0, assert mock_worksheet.append_rows.call_count == 0

    test_row_length: call with one ticker, get call_args[0][0] (the rows list), assert len(rows[0]) == 20

    test_none_in_row: call with {"AAPL": {"RSI": None}}, inspect rows[0][7] (RSI is column index 7 in the 20-col schema: Export Date=0, Ticker=1, Price=2, P/E=3, Forward P/E=4, P/B=5, EPS=6, RSI=7), assert rows[0][7] == ""

    Note: At Wave 0, import of src.utils.sheets_utils will FAIL because the module does not exist yet. Use pytest.importorskip or a try/except at module level to mark tests as xfail rather than causing collection errors:
    ```python
    pytest.importorskip("src.utils.sheets_utils", reason="sheets_utils not implemented yet (Wave 0)")
    ```
    Place this at the top of the file so the entire module is skipped gracefully when the source module is absent. Remove it in Wave 1 after the source module exists.
  </action>
  <verify>
    <automated>cd /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper && pytest tests/test_unit_sheets_utils.py -x -q 2>&1 | head -30</automated>
  </verify>
  <done>File exists, pytest collects it without ImportError or SyntaxError, all tests are skipped/xfail (not failed), at least 12 test functions defined</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append TestExportSheets class to tests/test_integration_routes.py</name>
  <files>tests/test_integration_routes.py</files>
  <behavior>
    - test_export_sheets_success: POST /api/export-sheets with valid tickers+data → 200 + {success: true, rows_added: 2}
    - test_export_sheets_no_body: POST /api/export-sheets with no JSON body → 400
    - test_export_sheets_empty_tickers: POST /api/export-sheets with tickers=[] → 400
    - test_export_sheets_no_creds: POST /api/export-sheets when sheets_utils raises FileNotFoundError → 500 + {success: false, error: contains "credentials"}
    - test_export_sheets_not_found: POST /api/export-sheets when gspread raises SpreadsheetNotFound → 500 + {success: false, error: contains "not found" or "not shared"}
    - test_export_sheets_api_error: POST /api/export-sheets when sheets_utils raises APIError → 500 + {success: false}
  </behavior>
  <action>
    Append a new class TestExportSheets at the END of tests/test_integration_routes.py (do not touch existing content). Use the existing `client` fixture from conftest.py.

    Class structure:
    ```python
    class TestExportSheets:
        """Integration tests for POST /api/export-sheets (Phase 31)."""

        _EXPORT_PAYLOAD = {
            "tickers": ["AAPL", "MSFT"],
            "data": {
                "AAPL": {"Price": "175.00", "RSI": "55.2"},
                "MSFT": {"Price": "420.00", "RSI": None},
            }
        }

        def test_export_sheets_success(self, client):
            ...

        def test_export_sheets_no_body(self, client):
            ...

        def test_export_sheets_empty_tickers(self, client):
            ...

        def test_export_sheets_no_creds(self, client):
            ...

        def test_export_sheets_not_found(self, client):
            ...

        def test_export_sheets_api_error(self, client):
            ...
    ```

    For test_export_sheets_success:
    - Mock "src.utils.sheets_utils.export_tickers_to_sheets" to return 2
    - Also mock "os.path.exists" to return True and env vars for creds path + spreadsheet ID
    - POST to /api/export-sheets with _EXPORT_PAYLOAD
    - Assert resp.status_code == 200, data["success"] is True, data["rows_added"] == 2

    For test_export_sheets_no_body:
    - POST to /api/export-sheets with no body (content_type="application/json", data=None)
    - Assert resp.status_code == 400

    For test_export_sheets_empty_tickers:
    - POST with {"tickers": [], "data": {}}
    - Assert resp.status_code == 400

    For test_export_sheets_no_creds:
    - Mock "src.utils.sheets_utils.export_tickers_to_sheets" to raise FileNotFoundError("Credentials file not found")
    - Assert resp.status_code == 500, data["success"] is False

    For test_export_sheets_not_found:
    - Mock "src.utils.sheets_utils.export_tickers_to_sheets" to raise gspread.exceptions.SpreadsheetNotFound()
    - Assert resp.status_code == 500, data["success"] is False
    - Note: import gspread at top of class or inside the test with try/except for graceful skip if gspread not installed

    For test_export_sheets_api_error:
    - Mock "src.utils.sheets_utils.export_tickers_to_sheets" to raise a ValueError("Sheets API error")
    - Assert resp.status_code in (400, 500), data["success"] is False

    Since the /api/export-sheets route does not exist yet in Wave 0, these tests will return 404. Use pytest.mark.integration on the class. At Wave 0 they will FAIL (404 vs 200/400/500) — that is expected and correct TDD behavior.
  </action>
  <verify>
    <automated>cd /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper && pytest tests/test_integration_routes.py::TestExportSheets -x -q 2>&1 | head -30</automated>
  </verify>
  <done>TestExportSheets class with 6 test methods appended to test_integration_routes.py, pytest collects the class without SyntaxError, tests may fail (404) but do not error during collection</done>
</task>

</tasks>

<verification>
pytest tests/test_unit_sheets_utils.py -q 2>&1 | tail -5   # all skipped/xfail, no errors
pytest tests/test_integration_routes.py::TestExportSheets -q 2>&1 | tail -5  # collects 6 tests
</verification>

<success_criteria>
- tests/test_unit_sheets_utils.py exists with at minimum 12 test function stubs and pytest.importorskip guard
- TestExportSheets class with 6 methods exists at the end of tests/test_integration_routes.py
- Neither file produces collection errors
- All subsequent plan verify commands reference tests defined in this plan
</success_criteria>

<output>
After completion, create .planning/phases/31-integration-with-googlesheets-for-local-webapp/31-01-SUMMARY.md
</output>
