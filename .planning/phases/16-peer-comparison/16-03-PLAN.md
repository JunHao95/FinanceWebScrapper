---
phase: 16-peer-comparison
plan: "03"
type: execute
wave: 3
depends_on:
  - "16-02"
files_modified: []
autonomous: false
requirements:
  - PEER-01
  - PEER-02
  - PEER-03
  - PEER-04
  - PEER-05
must_haves:
  truths:
    - "Scraping AAPL shows four percentile rows in the Peer Comparison section with badge-success or badge-danger on each"
    - "The peer group label lists at least two peer tickers by name"
    - "Show peers toggle reveals the raw peer table and hides it again on second click"
    - "When a ticker with no Finviz similar-stocks data is scraped, the section shows Peer Comparison: Unavailable in muted style"
    - "No JavaScript console errors are thrown during any of the above flows"
  artifacts:
    - path: "static/js/peerComparison.js"
      provides: "Verified working in browser end-to-end"
  key_links:
    - from: "Browser"
      to: "peerComparison.js renderIntoGroup"
      via: "Scrape button -> displayResults -> createTickerCard -> renderIntoGroup -> /api/peers"
      pattern: "manual browser verification"
---

<objective>
Human verification checkpoint: confirm the complete peer comparison flow works in a live browser session — spinner during load, percentile rows with badges on success, show/hide peer table toggle, and graceful failure state.

Purpose: Automated tests verify the backend route shape; this checkpoint verifies the rendered UI matches all five PEER success criteria.
Output: Human approval that all five PEER requirements are met in the browser.
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-peer-comparison/16-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Peer Comparison feature:
    - Backend: GET /api/peers?ticker=X with 30-min sector-scoped cache
    - Frontend: peerComparison.js async module with spinner, percentile rows, show/hide peer table
    - Wired into displayManager.js and index.html
  </what-built>
  <how-to-verify>
    1. Start the app: python webapp.py (or your usual start command)
    2. Open http://localhost:5000 in a browser
    3. Open the browser DevTools console (F12) to watch for JS errors

    Test A — Success path:
      a. Enter "AAPL" in the ticker field and click Run Analysis
      b. While results load, check the Deep Analysis section on the AAPL card
      c. Confirm you see "Peer Comparison" with a spinner or loading indicator briefly
      d. After data loads, confirm the collapsed header reads "Peer Comparison: X/4 above median"
      e. Click the header to expand — confirm four rows appear: P/E Ratio, P/B Ratio, ROE, Op. Margin
      f. Each row must show a percentile number (e.g. "45th percentile") and a coloured badge (green ABOVE MEDIAN or red BELOW MEDIAN)
      g. Confirm a "Comparable group: ..." line is visible listing peer ticker names
      h. Click "Show peers" — confirm a table appears with columns Ticker | P/E | P/B | ROE | Op. Margin and at least 2 data rows
      i. Click "Hide peers" (or "Show peers" again) — confirm the table hides

    Test B — Cache hit:
      a. Without reloading the page, scrape AAPL again (or scrape MSFT which is in the same Technology sector)
      b. Confirm the peer section populates nearly instantly (no visible spinner delay for same sector)

    Test C — Failure state (optional but recommended):
      a. Scrape a very obscure ticker that Finviz may not have similar stocks for (e.g. a small-cap ETF like "ARKK" or "BND")
      b. Confirm the section shows "Peer Comparison: Unavailable" in a muted/greyed style with no expand arrow

    Test D — No console errors:
      a. Confirm the DevTools console shows no red JS errors during any of the above tests

    All four tests must pass.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found (e.g. "spinner never disappears", "badge colours wrong", "peer table missing column")</resume-signal>
</task>

</tasks>

<verification>
Human confirms: spinner shown during load, four percentile rows with badges on success, peer group label visible, show/hide toggle works, failure state renders correctly, no JS console errors.
</verification>

<success_criteria>
Human types "approved" after validating all five PEER success criteria in a live browser session.
</success_criteria>

<output>
After completion, create `.planning/phases/16-peer-comparison/16-03-SUMMARY.md` following the summary template.
</output>
