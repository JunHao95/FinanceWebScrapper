---
phase: 13-financial-health-score
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified: []
autonomous: false
requirements:
  - FHLTH-01
  - FHLTH-02
  - FHLTH-03
  - FHLTH-04

must_haves:
  truths:
    - "A ticker card shows a financial health grade badge (A–F) in a collapsed 'Deep Analysis' section after scraping"
    - "Clicking the collapsed header expands four sub-score rows with individual letter grades"
    - "An explanation sentence is visible in the expanded panel"
    - "When a sub-score has missing data, a ⚠ flag appears on that row and the overall grade still renders"
    - "Session expand state persists after re-scraping (expanded card stays expanded)"
    - "window.pageContext.tickerData['AAPL'].healthScore is populated in the browser console"
  artifacts:
    - path: "static/js/healthScore.js"
      provides: "Working scoring module verified via browser"
    - path: "static/js/displayManager.js"
      provides: "createTickerCard() injects deep-analysis-group"
  key_links:
    - from: "browser ticker card"
      to: "div.deep-analysis-group"
      via: "displayManager.js createTickerCard() rendering healthScore.js output"
      pattern: "deep-analysis-group"
---

<objective>
Human verification checkpoint — confirm the financial health score feature works end-to-end in a real browser after a live scrape, covering all four FHLTH requirements.

Purpose: Quality gate before phase is marked complete. No code changes expected.
Output: Human approval to proceed to Phase 14.
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-financial-health-score/13-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Financial Health Score end-to-end in browser</name>
  <files>none — verification only, no file changes</files>
  <action>
Start the Flask dev server (`python webapp.py`) and run the six checks below in a browser at http://127.0.0.1:5000.

**Check 1 — Grade badge visible (FHLTH-01):**
1. Enter "AAPL" in the ticker input and click Run Analysis
2. After results load, navigate to the Stocks tab
3. Expand the AAPL ticker card
4. Scroll to the bottom of the card — confirm "🏥 Financial Health: [letter] ▼" appears with a coloured badge
5. Expected: grade badge visible, section starts collapsed

**Check 2 — Sub-scores expand (FHLTH-02):**
1. Click the "🏥 Financial Health" header
2. Confirm four rows appear: Liquidity, Leverage, Profitability, Growth
3. Each row shows a letter badge AND at least one raw metric value
4. Expected: all four rows visible with individual letters and numeric values

**Check 3 — Explanation sentence (FHLTH-03):**
1. With the panel expanded, confirm a plain-text sentence appears below the sub-scores
2. It should mention a positive and/or negative driver (e.g. "Strong Profitability offset by weak Leverage.")
3. Expected: non-empty explanation text visible

**Check 4 — Missing data warning (FHLTH-04):**
1. Scrape a ticker with potentially sparse data (e.g. "GME")
2. Expand the Deep Analysis section
3. If any row has ⚠ appended (e.g. "Growth ⚠"), the warning is working
4. Confirm the overall grade badge is still shown even when some rows show N/A
5. Note: if the ticker has complete data, this check passes by code inspection

**Check 5 — Session expand state persists (FHLTH-01):**
1. Expand the AAPL Deep Analysis panel
2. Scrape AAPL again without reloading the page
3. After re-scrape, confirm the panel is still expanded
4. Expected: expand state preserved across scrapes within the same session

**Check 6 — pageContext populated:**
1. After a successful AAPL scrape, open DevTools Console
2. Run: `window.pageContext.tickerData['AAPL'].healthScore`
3. Expected: object with keys `grade` (A–F string), `subScores` (array of 4), `explanation` (string)
  </action>
  <verify>
    <automated>pytest tests/ -x -q</automated>
  </verify>
  <done>Human confirms all six checks pass. Phase 13 is complete and ready for Phase 14 planning.</done>
  <resume-signal>Type "approved" if all six checks pass. Describe any failing check with observed vs expected behaviour if issues are found.</resume-signal>
</task>

</tasks>

<verification>
All six browser checks pass: grade badge renders, sub-scores expand, explanation visible, missing-data ⚠ flag works, session state persists, pageContext populated correctly.
</verification>

<success_criteria>
Human approves all six checks. Phase 13 is complete.
</success_criteria>

<output>
After approval, create `.planning/phases/13-financial-health-score/13-02-SUMMARY.md`
</output>
