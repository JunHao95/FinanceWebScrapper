---
phase: 26-ml-finance-notebook-integration
plan: "05"
type: execute
wave: 4
depends_on: ["26-03"]
files_modified:
  - templates/index.html
autonomous: true
requirements: [ML-TAB-01]

must_haves:
  truths:
    - "A fifth tab button 'ML Signals' is visible in the tab bar in the rendered HTML"
    - "div#mlSignalsTabContent exists as a tab-content div in index.html"
    - "div#mlSignalsPcaSection exists inside mlSignalsTabContent for PCA charts"
    - "script tag for /static/js/mlSignals.js appears in index.html after tradingIndicators.js"
    - "Tab button calls switchTab('mlsignals') onclick"
  artifacts:
    - path: "templates/index.html"
      provides: "5th tab button + mlSignalsTabContent div + mlSignalsPcaSection div + script tag"
      contains: "mlSignalsTab"
  key_links:
    - from: "templates/index.html #mlSignalsTab button"
      to: "static/js/tabs.js switchTab('mlsignals')"
      via: "onclick=\"switchTab('mlsignals')\""
      pattern: "switchTab.*mlsignals"
    - from: "templates/index.html script"
      to: "static/js/mlSignals.js"
      via: "script src tag"
      pattern: "mlSignals.js"
---

<objective>
Add the HTML scaffolding for the ML Signals tab: the 5th tab button, the `mlSignalsTabContent` div, the `mlSignalsPcaSection` section div, and the script tag for `mlSignals.js`. This is a pure HTML change — parallel with Plan 04 (no JS file overlap).

Purpose: Give `mlSignals.js` (Plan 04) and `tabs.js` the DOM elements they expect: `#mlSignalsTab`, `#mlSignalsTabContent`, `#mlSignalsPcaSection`.
Output: `templates/index.html` updated with 3 HTML additions and 1 script tag.
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-ml-finance-notebook-integration/26-CONTEXT.md
@.planning/phases/26-ml-finance-notebook-integration/26-03-SUMMARY.md
@templates/index.html

<interfaces>
<!-- HTML patterns to mirror. Extracted from templates/index.html. -->

Existing 4th tab button (line ~151):
```html
<button class="tab-button" onclick="switchTab('tradingindicators')" id="tradingIndicatorsTab">Trading Indicators</button>
```

Existing tradingIndicators content div (line ~232):
```html
<div class="tab-content" id="tradingIndicatorsTabContent">
    <div id="tradingIndicatorsContent"></div>
</div>
```

Existing script tag (line ~1345):
```html
<script src="/static/js/tradingIndicators.js"></script>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5th tab button, content div, PCA section, and script tag to index.html</name>
  <files>templates/index.html</files>
  <action>
    Read templates/index.html first. Make FOUR targeted surgical edits using the Edit tool — do NOT rewrite the file:

    Edit 1 — Tab button:
    Find the line with `id="tradingIndicatorsTab"` (the 4th tab button).
    After that line, add:
    `                <button class="tab-button" onclick="switchTab('mlsignals')" id="mlSignalsTab">ML Signals</button>`

    Edit 2 — Tab content div:
    Find the `<div class="tab-content" id="tradingIndicatorsTabContent">` block.
    After the closing `</div>` of that block, add:
    ```html
                <div class="tab-content" id="mlSignalsTabContent">
                    <div id="mlSignalsPcaSection"></div>
                    <div id="mlSignalsContent"></div>
                </div>
    ```

    Edit 3 — Script tag:
    Find `<script src="/static/js/tradingIndicators.js"></script>`.
    After that line, add:
    `    <script src="/static/js/mlSignals.js"></script>`

    That's all 3 edits. Do NOT add any CSS — existing tab-button and tab-content styles apply automatically.

    After all edits, verify the HTML is well-formed by checking that every opened div has a matching close.
  </action>
  <verify>
    <automated>grep -n "mlSignalsTab\|mlSignalsTabContent\|mlSignalsPcaSection\|mlSignals.js" /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper/templates/index.html</automated>
  </verify>
  <done>grep returns 4 matches: mlSignalsTab (button id), mlSignalsTabContent (div id), mlSignalsPcaSection (div id), mlSignals.js (script src). Flask app still starts without Jinja2 template errors: `python -c "import webapp; print('OK')"` prints OK.</done>
</task>

</tasks>

<verification>
All 4 IDs present:
```
grep -c "mlSignalsTab\|mlSignalsTabContent\|mlSignalsPcaSection\|mlSignals.js" /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper/templates/index.html
```
Expected output: 4

Flask template renders without error:
```
cd /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper && python -c "import webapp; ctx = webapp.app.test_request_context(); ctx.push(); from flask import render_template; render_template('index.html'); print('Template OK')" 2>&1 | tail -5
```
</verification>

<success_criteria>
- Button `id="mlSignalsTab"` with `onclick="switchTab('mlsignals')"` present in index.html tab bar
- `div#mlSignalsTabContent` with class "tab-content" contains `div#mlSignalsPcaSection`
- `script src="/static/js/mlSignals.js"` present after tradingIndicators.js script tag
- Jinja2 template renders without error
- Existing tabs (stocks, analytics, autoanalysis, tradingindicators) are unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/26-ml-finance-notebook-integration/26-05-SUMMARY.md`
</output>
