---
phase: 26-ml-finance-notebook-integration
plan: "06"
type: execute
wave: 5
depends_on: ["26-04", "26-05"]
files_modified: []
autonomous: false
requirements: [ML-DIR-01, ML-PCA-01, ML-REG-01, ML-CRED-01, ML-LSTM-01, ML-TAB-01]

must_haves:
  truths:
    - "ML Signals tab button is visible and clickable in the browser"
    - "After scraping a ticker (e.g. AAPL), clicking ML Signals tab shows a per-ticker card with at least Direction Signal and K-Means Regime results"
    - "No browser console errors on tab activation"
    - "LSTM card shows either results (local) or the cloud-disabled placeholder (Render)"
    - "Credit Risk Score shows a percentage or the class-imbalance fallback message"
    - "Multi-ticker scrape (e.g. AAPL + MSFT) shows PCA scree plot and heatmap above the ticker cards"
  artifacts:
    - path: "static/js/mlSignals.js"
      provides: "Renders ML cards in browser"
      contains: "window.MLSignals"
    - path: "templates/index.html"
      provides: "ML Signals tab DOM elements"
      contains: "mlSignalsTab"
  key_links:
    - from: "Browser ML Signals tab"
      to: "/api/ml_signals route"
      via: "fetch calls in mlSignals.js fetchForTicker"
      pattern: "fetch.*api/ml_signals"
---

<objective>
Human visual verification that the ML Signals tab is fully functional end-to-end: tab renders, API calls succeed, all ML feature cards display correct content, no console errors.

Purpose: Confirm the complete browser-to-API-to-model-to-render pipeline works before phase is marked complete.
Output: Human approval or list of specific issues for gap closure.
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-ml-finance-notebook-integration/26-CONTEXT.md
@.planning/phases/26-ml-finance-notebook-integration/26-04-SUMMARY.md
@.planning/phases/26-ml-finance-notebook-integration/26-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and start Flask dev server</name>
  <files></files>
  <action>
    Run the complete test suite (excluding E2E and slow network tests) to confirm all automated tests pass:
    ```
    cd /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper && python -m pytest tests/ -x -q --ignore=tests/test_e2e_golden_path.py -k "not slow" 2>&1 | tail -20
    ```

    If all tests pass, start the Flask development server:
    ```
    cd /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper && python webapp.py
    ```
    The server runs on http://127.0.0.1:5000 by default.

    Report the test results and the server URL to the user.
  </action>
  <verify>
    <automated>cd /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper && python -m pytest tests/ -x -q --ignore=tests/test_e2e_golden_path.py -k "not slow" 2>&1 | tail -5</automated>
  </verify>
  <done>pytest exits with 0 failures (warnings acceptable). Flask server starts on port 5000.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete ML Signals tab:
    - 5th tab button "ML Signals" in the results tab bar (alongside Stocks / Analytics / Auto Analysis / Trading Indicators)
    - Per-ticker cards showing: Direction Signal badge + feature importance chart, K-Means Regime vs HMM comparison, Credit Risk percentage, LSTM card (results locally / cloud-disabled placeholder on Render)
    - PCA Decomposition section above ticker cards when 2+ tickers are analysed (scree plot + factor loadings heatmap)
  </what-built>
  <how-to-verify>
    Single-ticker flow (test Direction Signal, Regime, Credit Risk, LSTM):
    1. Open http://127.0.0.1:5000 in browser
    2. Enter ticker "AAPL" in the search box and click Run Analysis
    3. Wait for results to load (~5–15 seconds including ML training)
    4. Click the "ML Signals" tab in the results area
    5. Verify: an AAPL card appears with "Training ML models..." loading state, then results populate
    6. Check Direction Signal: badge shows "Bullish" or "Bearish" with a confidence % (e.g. "67% Bullish") and a horizontal bar chart of top 5 features
    7. Check K-Means Regime: two badges showing HMM regime and K-Means regime, with agree/diverge note
    8. Check Credit Risk Score: a percentage displayed OR "Credit model could not train" fallback message
    9. Check LSTM: either a badge + loss curve chart (if running locally with TF) OR grey card "Deep learning disabled on cloud"
    10. Open browser DevTools Console (F12) — confirm zero console errors

    Multi-ticker flow (test PCA):
    11. Clear results and enter both "AAPL" and "MSFT" as tickers
    12. Click Run Analysis, wait for results
    13. Click "ML Signals" tab
    14. Verify: a PCA section appears ABOVE the ticker cards, showing a scree bar chart and a factor loadings heatmap with tickers on y-axis

    Expected visual outcome: Dark Catppuccin theme (#1e1e2e background), green (#2ecc71) for bullish/positive signals, red (#e74c3c) for bearish/warning, grey (#7f849c) for unavailable/neutral.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues (e.g. "credit card missing" or "console error: Cannot read properties of undefined")</resume-signal>
</task>

</tasks>

<verification>
Automated pre-checkpoint gate:
```
cd /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper && python -m pytest tests/test_unit_ml_signals.py tests/test_integration_routes.py -x -q 2>&1 | tail -10
```
All ml_signals tests pass.
</verification>

<success_criteria>
- All automated tests pass (zero failures)
- ML Signals tab visible and activates without console errors
- Direction Signal, Regime, Credit Risk, LSTM sections all render (content or graceful fallback)
- PCA section renders for 2+ ticker analysis
- Human approves via "approved" signal
</success_criteria>

<output>
After human approval, create `.planning/phases/26-ml-finance-notebook-integration/26-06-SUMMARY.md`
</output>
