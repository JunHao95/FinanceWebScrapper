---
phase: 26-ml-finance-notebook-integration
plan: "02"
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/analytics/ml_signals.py
autonomous: true
requirements: [ML-DIR-01, ML-PCA-01, ML-REG-01, ML-CRED-01, ML-LSTM-01]

must_haves:
  truths:
    - "compute_ml_direction_signal('AAPL') returns {signal, confidence, traces, layout} with no NaN"
    - "compute_pca_decomposition(['AAPL']) returns {pca_available: False}"
    - "compute_pca_decomposition(['AAPL','MSFT']) returns {variance_explained: [f1,f2,f3], scree_traces, heatmap_traces}"
    - "compute_kmeans_regime('AAPL') returns {current_regime: one of Bull/Bear/Volatile/Ranging, hmm_regime, models_agree}"
    - "compute_credit_risk_score('AAPL', ratios) returns {p_distress in [0,1], top_factors list, caveat string}"
    - "compute_lstm_direction_signal with KERAS_AVAILABLE=False returns {lstm_available: False}"
    - "All numpy types converted to Python builtins before return"
  artifacts:
    - path: "src/analytics/ml_signals.py"
      provides: "All 5 ML feature compute functions"
      exports: ["compute_ml_direction_signal", "compute_pca_decomposition", "compute_kmeans_regime", "compute_credit_risk_score", "compute_lstm_direction_signal", "KERAS_AVAILABLE"]
  key_links:
    - from: "src/analytics/ml_signals.py"
      to: "src/analytics/trading_indicators.fetch_ohlcv"
      via: "from src.analytics.trading_indicators import fetch_ohlcv"
      pattern: "fetch_ohlcv"
    - from: "src/analytics/ml_signals.py"
      to: "src/analytics/regime_detection.RegimeDetector"
      via: "try/except wrapped call inside compute_kmeans_regime"
      pattern: "RegimeDetector"
    - from: "src/analytics/ml_signals.py"
      to: "tensorflow.keras"
      via: "module-level try/except ImportError sets KERAS_AVAILABLE flag"
      pattern: "KERAS_AVAILABLE"
---

<objective>
Implement `src/analytics/ml_signals.py` — the core ML analytics module for Phase 26. Contains five compute functions: RF Direction Signal (M1+M5), PCA Portfolio Decomposition (M2), K-Means Market Regime (M4), Ensemble Credit Risk Score (M3), and environment-gated LSTM Direction Signal (M6).

Purpose: Provide the pure-Python backend analytics that the Flask route (Plan 03) and tests (Plan 01) depend on.
Output: `src/analytics/ml_signals.py` — new file, all functions returning plain-Python dicts (numpy converted to lists).
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-ml-finance-notebook-integration/26-CONTEXT.md
@.planning/phases/26-ml-finance-notebook-integration/26-RESEARCH.md
@.planning/phases/26-ml-finance-notebook-integration/26-01-SUMMARY.md
@src/analytics/rl_models.py
@src/analytics/trading_indicators.py
@src/analytics/regime_detection.py

<interfaces>
<!-- Key imports the executor needs. Extracted from codebase. -->

From src/analytics/trading_indicators.py:
```python
def fetch_ohlcv(ticker: str, days: int, auto_adjust: bool = True) -> pd.DataFrame:
    """Returns DataFrame with columns: Open, High, Low, Close, Volume. DatetimeIndex."""
```

From src/analytics/regime_detection.py:
```python
class RegimeDetector:
    def fit(self, returns: pd.Series) -> 'RegimeDetector': ...
    # After fit: self.current_regime -> str ("Bullish"/"Bearish")
    # Returns dict from .predict() or use .current_regime attribute
```

From webapp.py:
```python
def convert_numpy_types(data): ...   # line 173 — recursively converts np types to Python builtins
def is_cloud_environment() -> bool: ...  # line 92 — checks RENDER/RENDER_SERVICE_ID env vars
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement sklearn ML features — Direction Signal, PCA, K-Means, Credit Risk</name>
  <files>src/analytics/ml_signals.py</files>
  <behavior>
    Direction Signal:
    - compute_ml_direction_signal(ticker: str) -> dict
    - fetch_ohlcv(ticker, days=500); check len(df) < 265 → return {"insufficient_data": True, "signal": None, "confidence": None, "traces": [], "layout": {}}
    - Build feature matrix: _compute_momentum_features(df) + compute_technical_indicators(df); concat; drop NaN; need 25-day forward return target label
    - Chronological split at 80%: X_train, X_test = X.iloc[:split], X.iloc[split:]; scaler.fit_transform on train only
    - RF: RandomForestClassifier(n_estimators=50, max_depth=5, random_state=42, n_jobs=-1).fit(X_train_sc, y_train)
    - predict_proba on X_latest (last clean row after scaling); positive_class_idx = list(rf.classes_).index(1)
    - Return: signal ('Bullish'/'Bearish'), confidence (float), traces (Plotly horizontal bar of top 5 feature importances), layout (DARK_LAYOUT)

    PCA Decomposition:
    - compute_pca_decomposition(tickers: list) -> dict
    - If len(tickers) < 2: return {"pca_available": False, "reason": "single_ticker"}
    - Fetch 365-day OHLCV for each ticker using fetch_ohlcv; build return matrix (pct_change).dropna()
    - StandardScaler on full matrix (no train/test split needed — PCA is unsupervised, no target leakage risk)
    - PCA(n_components=min(3, len(tickers))); fit_transform
    - Return: pca_available=True, variance_explained=[evr list], scree_traces=[bar trace dict], heatmap_traces=[heatmap trace dict with tickers as y-axis rows, PCs as x-axis columns], layout=DARK_LAYOUT
    - Label PC1="Market Factor", PC2="Sector Tilt", PC3="Curvature" in layout annotations

    K-Means Regime:
    - compute_kmeans_regime(ticker: str) -> dict
    - fetch_ohlcv(ticker, days=365); compute rolling 20-day mean return + rolling 20-day annualised vol (std * sqrt(252))
    - Drop NaN rows; KMeans(n_clusters=4, n_init=10, random_state=42).fit(X_regime)
    - _label_clusters(kmeans, feature_cols) assigns Bull/Bear/Volatile/Ranging based on centroid mean_return + ann_vol
    - current_regime_label = cluster_name_map[int(labels[-1])]
    - Wrap RegimeDetector().fit(returns) in try/except → hmm_regime = result or None on failure
    - models_agree = True if hmm_regime matches current_regime (map HMM "Bullish"→"Bull", "Bearish"→"Bear")
    - Return: current_regime, hmm_regime (str or None), models_agree (bool or None), regime_timeline_traces=[scatter trace with colour per day], layout=DARK_LAYOUT

    Credit Risk Score:
    - compute_credit_risk_score(ticker: str, ratios: dict) -> dict
    - Uses ratios dict keys: current_ratio, debt_to_equity, return_on_equity, operating_margin, revenue_growth, earnings_growth (use .get() with fallback=0 for missing keys)
    - Construct synthetic training dataset: 200 synthetic companies with ratios sampled from realistic distributions (np.random.seed(42)); distress label=1 if earnings_growth < -0.05 AND debt_to_equity > 1.5, else 0
    - Check y.sum() and (len(y)-y.sum()): if either is 0, return {"p_distress": None, "degenerate_labels": True, "insufficient_data": True}
    - RandomForestClassifier(n_estimators=50, max_depth=4, random_state=42).fit(X_synth, y_synth)
    - Compute p_distress for the real ticker's ratio vector
    - top_factors: top 3 features by rf.feature_importances_ × abs(feature_value - training_mean), with sign
    - Return: p_distress (float 0-1), top_factors (list of dicts with name, value, contribution), caveat="Model trained on ratio thresholds — indicative only. Not a credit rating."
  </behavior>
  <action>
    Create src/analytics/ml_signals.py. Structure:

    1. Module docstring explaining all 5 functions
    2. Standard imports: numpy, pandas, logging, typing
    3. sklearn imports: RandomForestClassifier, KMeans, PCA, StandardScaler (unconditional — sklearn is always available)
    4. Keras try/except at module level:
       ```python
       try:
           from tensorflow import keras
           KERAS_AVAILABLE = True
       except ImportError:
           keras = None
           KERAS_AVAILABLE = False
       ```
    5. DARK_LAYOUT constant:
       ```python
       DARK_LAYOUT = {"paper_bgcolor": "#1e1e2e", "plot_bgcolor": "#1e1e2e", "font": {"color": "#cdd6f4"}}
       ```
    6. Private helpers: _compute_momentum_features(df), compute_technical_indicators(df), _rsi(series, window), _label_clusters(kmeans, feature_cols)
    7. The four sklearn functions in order: compute_ml_direction_signal, compute_pca_decomposition, compute_kmeans_regime, compute_credit_risk_score
    8. ALL numpy return values must be converted: use .tolist() on arrays, float() on scalar numpy types

    Anti-leakage guard (CRITICAL — from M1 notebook):
    - chronological split: split_idx = int(len(X) * 0.8); X_train = X.iloc[:split_idx], X_test = X.iloc[split_idx:]
    - scaler.fit_transform on X_train only; scaler.transform on X_test and X_latest
    - NEVER call scaler.fit on full dataset

    Fetch call for Direction Signal: fetch_ohlcv(ticker, days=500) to ensure Ret240 window fits
    Fetch call for K-Means: fetch_ohlcv(ticker, days=365)
    Fetch call for PCA: fetch_ohlcv(t, days=365) for each ticker in list
  </action>
  <verify>
    <automated>cd /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper && python -m pytest tests/test_unit_ml_signals.py -k "not lstm" -x -q 2>&1 | tail -15</automated>
  </verify>
  <done>All 8 non-LSTM unit tests pass (GREEN). No test marked ERROR. Import succeeds. The 2 LSTM tests remain SKIP if keras unavailable or pass if available.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement compute_lstm_direction_signal with environment gate</name>
  <files>src/analytics/ml_signals.py</files>
  <behavior>
    - compute_lstm_direction_signal(ticker: str) -> dict
    - First check: if not KERAS_AVAILABLE: return {"lstm_available": False}
    - fetch_ohlcv(ticker, days=500); compute daily returns; check len < 100 → return {"lstm_available": False, "insufficient_data": True}
    - Construct sequences: make_sequences(returns_array, seq_len=20) → X shape (N, 20, 1), y shape (N,)
    - Chronological split at 80% on sequence arrays (not iloc — use array slicing)
    - Scale returns on train: scaler.fit_transform(X_train.reshape(-1,1)).reshape(X_train.shape)
    - Build model: keras.Sequential([keras.layers.Input(shape=(20,1)), keras.layers.LSTM(64), keras.layers.Dense(1, activation='sigmoid')])
    - model.compile(optimizer='adam', loss='binary_crossentropy')
    - history = model.fit(X_train_sc, y_train, epochs=20, batch_size=32, validation_split=0.1, verbose=0)
    - Predict on latest 20-day window; confidence = float(model.predict(X_latest, verbose=0)[0][0])
    - signal = 'Bullish' if confidence >= 0.5 else 'Bearish'
    - loss_curve_traces = [{"type": "scatter", "x": list(range(len(history.history['loss']))), "y": [float(v) for v in history.history['loss']], "name": "Train Loss"}]
    - Return: {"lstm_available": True, "signal": signal, "confidence": confidence, "loss_curve_traces": loss_curve_traces, "layout": DARK_LAYOUT}
  </behavior>
  <action>
    Add compute_lstm_direction_signal to the EXISTING src/analytics/ml_signals.py created in Task 1. Do NOT rewrite the file — append or insert at the appropriate position before the module ends.

    Also add a private helper make_sequences(arr, seq_len=20) that returns (X, y) as numpy arrays.

    The Keras layers must be referenced as `keras.layers.Input`, `keras.layers.LSTM`, `keras.layers.Dense` — these are the stable Keras 3.x API paths confirmed in 26-RESEARCH.md.

    After adding the function, run the full unit test suite to confirm all 10 tests now pass (or skip for LSTM if keras not available locally — that is acceptable).
  </action>
  <verify>
    <automated>cd /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper && python -m pytest tests/test_unit_ml_signals.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <done>All 10 unit tests in test_unit_ml_signals.py pass or skip (no FAIL, no ERROR). src/analytics/ml_signals.py imports cleanly: `python -c "from src.analytics.ml_signals import compute_ml_direction_signal, KERAS_AVAILABLE; print('OK', KERAS_AVAILABLE)"` prints OK.</done>
</task>

</tasks>

<verification>
Full unit test run:
```
cd /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper && python -m pytest tests/test_unit_ml_signals.py -v 2>&1 | tail -20
```
Import smoke test:
```
cd /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper && python -c "from src.analytics.ml_signals import compute_ml_direction_signal, compute_pca_decomposition, compute_kmeans_regime, compute_credit_risk_score, compute_lstm_direction_signal, KERAS_AVAILABLE; print('All imports OK, KERAS_AVAILABLE =', KERAS_AVAILABLE)"
```
Existing test suite unaffected:
```
cd /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper && python -m pytest tests/ -x -q --ignore=tests/test_e2e_golden_path.py -k "not slow" 2>&1 | tail -10
```
</verification>

<success_criteria>
- src/analytics/ml_signals.py exists with 5 compute functions + KERAS_AVAILABLE flag
- All sklearn functions return plain-Python dicts (no numpy scalar types)
- Chronological split (shuffle=False) enforced in all training paths
- KERAS_AVAILABLE=False path returns {"lstm_available": False} without importing TF
- All 10 unit tests pass or skip (zero FAIL, zero ERROR)
</success_criteria>

<output>
After completion, create `.planning/phases/26-ml-finance-notebook-integration/26-02-SUMMARY.md`
</output>
