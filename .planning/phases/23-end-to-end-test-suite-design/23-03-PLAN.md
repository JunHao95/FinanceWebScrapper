---
phase: 23-end-to-end-test-suite-design
plan: 03
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - tests/test_unit_options_pricer.py
  - tests/test_unit_rl_models.py
  - tests/test_unit_financial_analytics.py
  - tests/test_unit_ml_models.py
  - tests/test_regression_indicators.py
  - tests/test_regression_stochastic.py
  - tests/fixtures/volume_profile_ohlcv.csv
  - tests/fixtures/order_flow_ohlcv.csv
  - tests/fixtures/heston_market_prices.json
  - scripts/generate_fixtures.py
autonomous: true
requirements:
  - TEST-03
  - TEST-05

must_haves:
  truths:
    - "options_pricer, rl_models, financial_analytics, and ml_models each have at least one happy-path and one edge-case unit test"
    - "Volume Profile POC/VAH/VAL regression test pins expected values on frozen OHLCV data"
    - "Order Flow cumulative delta regression test pins expected values on frozen OHLCV data"
    - "Heston calibration regression test verifies RMSE below threshold on frozen market prices"
    - "HMM regime detection regression test identifies RISK_OFF in March 2020 on the existing spy_2017_2021.npy fixture"
    - "All regression tests use pytest.approx with appropriate tolerance (tight for pure math, loose for stochastic)"
    - "No test makes a live network call"
  artifacts:
    - path: "tests/test_unit_options_pricer.py"
      provides: "Unit tests for black_scholes, heston_price, merton_price functions"
      min_lines: 40
    - path: "tests/test_unit_rl_models.py"
      provides: "Unit tests for RL module functions"
      min_lines: 30
    - path: "tests/test_unit_financial_analytics.py"
      provides: "Unit tests for FinancialAnalytics class methods"
      min_lines: 30
    - path: "tests/test_unit_ml_models.py"
      provides: "Unit tests for ml_models module functions (ROADMAP requirement TEST-03)"
      min_lines: 20
    - path: "tests/test_regression_indicators.py"
      provides: "Regression tests for Volume Profile and Order Flow"
      min_lines: 40
    - path: "tests/test_regression_stochastic.py"
      provides: "Regression tests for Heston calibration and HMM regime detection"
      min_lines: 40
    - path: "tests/fixtures/volume_profile_ohlcv.csv"
      provides: "Frozen OHLCV data for Volume Profile regression"
    - path: "tests/fixtures/order_flow_ohlcv.csv"
      provides: "Frozen OHLCV data for Order Flow regression"
  key_links:
    - from: "tests/test_regression_indicators.py"
      to: "src/analytics/trading_indicators.py"
      via: "compute_volume_profile, compute_order_flow function calls"
      pattern: "compute_(volume_profile|order_flow)"
    - from: "tests/test_regression_stochastic.py"
      to: "src/analytics/regime_detection.py"
      via: "RegimeDetector class"
      pattern: "RegimeDetector"
---

<objective>
Fill unit test gaps for options_pricer, rl_models, financial_analytics, and ml_models modules, and create regression tests that pin expected outputs for Volume Profile, Order Flow, Heston calibration, and HMM regime detection using frozen fixture data.

Purpose: Ensures analytics modules have baseline test coverage and critical computations are regression-tested against known-correct outputs so any drift triggers a failure.
Output: 4 new unit test files, 2 regression test files, 3 fixture data files, fixture generation script.
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-end-to-end-test-suite-design/23-CONTEXT.md
@.planning/phases/23-end-to-end-test-suite-design/23-RESEARCH.md
@.planning/phases/23-end-to-end-test-suite-design/23-01-SUMMARY.md
@tests/conftest.py
@src/analytics/trading_indicators.py
@src/derivatives/options_pricer.py
@src/analytics/regime_detection.py
@src/analytics/financial_analytics.py
@src/analytics/rl_models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit test files for uncovered analytics modules (including ml_models)</name>
  <files>tests/test_unit_options_pricer.py, tests/test_unit_rl_models.py, tests/test_unit_financial_analytics.py, tests/test_unit_ml_models.py</files>
  <action>
Create four new unit test files. All tests get `@pytest.mark.unit`. No live network calls — use synthetic deterministic inputs only.

**tests/test_unit_options_pricer.py:**
Read `src/derivatives/options_pricer.py` to find all exported functions. Write tests for:
- `black_scholes(S, K, T, r, sigma, option_type)`:
  - Happy path: ATM call (S=K=100, T=1, r=0.05, sigma=0.2) should be approx 10.45 (rel=1e-2)
  - Edge case: Deep ITM call (S=100, K=50) price >= intrinsic (50.0)
  - Edge case: Put-call parity — call_price - put_price == S - K*exp(-rT) (within tolerance)
- `heston_cf_price` (or equivalent Heston pricer function):
  - Happy path: ATM with standard params should produce a reasonable price (5-15 range for ATM)
  - Edge case: Very low vol-of-vol (sigma_v=0.001) should converge to BS price
- Any other exported pricing functions (merton_price, etc.) — at least one test each

**tests/test_unit_rl_models.py:**
Read `src/analytics/rl_models.py` to find all exported functions/classes. Write tests for:
- Value iteration / policy iteration — small 2-3 state MDP with known optimal policy
- Edge case: Single-state MDP or trivial rewards
- Any gridworld or portfolio rotation helper functions — at least one test each

**tests/test_unit_financial_analytics.py:**
Read `src/analytics/financial_analytics.py` (or wherever FinancialAnalytics lives) to find all methods. Write tests for:
- Financial health score computation — mock input data dict with known metrics, verify grade output
- Edge case: Missing metrics should degrade gracefully (partial score, not crash)
- DCF valuation — mock FCF data, verify intrinsic value computation
- Any other exported methods — at least one test each

**tests/test_unit_ml_models.py (ROADMAP requirement TEST-03 — ml_models coverage):**
NOTE: `src/analytics/ml_models.py` does NOT exist as a standalone file in the codebase. The ROADMAP references it but the functionality was likely absorbed into other modules (regime_detection, financial_analytics). The executor MUST:
1. Search the codebase for any file named `ml_models.py` or containing ML model classes (e.g., `grep -r "class.*Model" src/` or `grep -r "ml_models" webapp.py`)
2. If a `ml_models.py` file exists anywhere (different path than `src/analytics/`), write unit tests for its exported functions — at least one happy-path and one edge-case test per function
3. If NO `ml_models.py` file exists anywhere, check if regime_detection.py or financial_analytics.py contain ML model functionality (HMM, classifiers, etc.) that isn't already covered by the other three test files. Write tests for any uncovered ML functions in `test_unit_ml_models.py`.
4. If all ML functionality is genuinely covered by other test files (test_unit_financial_analytics.py, existing test_regime_detection.py), create `test_unit_ml_models.py` as a thin file with a module docstring explaining: "ml_models.py referenced in ROADMAP does not exist as a standalone module. ML functionality is covered by: test_regime_detection.py (HMM), test_unit_financial_analytics.py (scoring/DCF). This file exists to satisfy the TEST-03 requirement traceability." Include at least one re-export or cross-reference test that imports from the covering modules to prove they are importable.

For all files: follow the naming convention `test_unit_*.py` per CONTEXT.md. Use `pytest.approx()` for float comparisons. Do NOT import webapp — import analytics modules directly.
  </action>
  <verify>
    <automated>cd /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper && pytest tests/test_unit_options_pricer.py tests/test_unit_rl_models.py tests/test_unit_financial_analytics.py tests/test_unit_ml_models.py -m unit -q --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>Four unit test files exist. Each module has at least one happy-path and one edge-case test (or documented justification for ml_models). All pass with `pytest -m unit`.</done>
</task>

<task type="auto">
  <name>Task 2: Create fixture data and regression tests for indicators and stochastic models</name>
  <files>scripts/generate_fixtures.py, tests/fixtures/volume_profile_ohlcv.csv, tests/fixtures/order_flow_ohlcv.csv, tests/fixtures/heston_market_prices.json, tests/test_regression_indicators.py, tests/test_regression_stochastic.py</files>
  <action>
**Step 1: Create fixture generation script and generate fixture data.**

Create `scripts/generate_fixtures.py` that:
- Fetches AAPL 6-month OHLCV via yfinance and saves to `tests/fixtures/volume_profile_ohlcv.csv` and `tests/fixtures/order_flow_ohlcv.csv` (can be same data)
- Creates `tests/fixtures/heston_market_prices.json` with a small set of synthetic market option prices (5-10 strike/maturity combos with realistic prices — no live fetch needed, just manually constructed)
- Prints the computed expected values (POC, VAH, VAL, cumulative delta last value) for hardcoding into regression tests

Run the script once to generate fixture files. Commit the fixture files to git. If yfinance fetch fails, create a synthetic OHLCV DataFrame with realistic AAPL-like data (150 rows, Open/High/Low/Close/Volume columns, prices in 140-170 range).

**Step 2: Create tests/test_regression_indicators.py.**

All tests get `@pytest.mark.regression`.

- `test_volume_profile_poc_regression`: Load volume_profile_ohlcv.csv, call `compute_volume_profile()`, assert POC/VAH/VAL match pinned values with `pytest.approx(expected, abs=0.10)` (tight — pure deterministic math)
- `test_volume_profile_signal_regression`: Assert the signal field is either "Price inside value area" or "Price outside value area"
- `test_order_flow_cumulative_delta_regression`: Load order_flow_ohlcv.csv, call `compute_order_flow()`, assert last cumulative delta value matches pinned value with `pytest.approx(expected, rel=0.01)`
- `test_order_flow_no_nan`: Assert no NaN values in cumulative_delta series
- `test_order_flow_divergence_fields`: Assert result contains divergence flag and slope values

**Step 3: Create tests/test_regression_stochastic.py.**

All tests get `@pytest.mark.regression`.

- `test_heston_calibration_convergence`: Load heston_market_prices.json, run calibration with fixed initial params, assert RMSE < threshold (use `rel=0.10` = 10% tolerance per RESEARCH.md pitfall 3)
- `test_hmm_regime_march_2020`: Load spy_2017_2021.npy fixture (already exists), set `np.random.seed(42)`, run RegimeDetector, assert March 2020 period is classified as high-volatility/RISK_OFF. Use loose assertion — check that the majority of March 2020 trading days are in the stressed regime.
- `test_hmm_regime_deterministic_seed`: Run RegimeDetector twice with same seed on same data, assert outputs are identical

**Tolerance guidelines (Claude's discretion per CONTEXT.md):**
- Volume Profile POC/VAH/VAL: abs=0.10 (deterministic, price-level precision)
- Order Flow cumulative delta: rel=0.01 (deterministic float accumulation)
- Heston RMSE: rel=0.10 (optimizer-sensitive)
- HMM regime: majority-vote assertion (stochastic — check >60% of March 2020 days are stressed)
  </action>
  <verify>
    <automated>cd /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper && pytest tests/test_regression_indicators.py tests/test_regression_stochastic.py -m regression -q --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>Fixture files exist in tests/fixtures/. Regression tests pin Volume Profile POC/VAH/VAL, Order Flow cumulative delta, Heston calibration RMSE, and HMM March 2020 regime. All pass with `pytest -m regression`.</done>
</task>

</tasks>

<verification>
- `pytest -m unit tests/test_unit_*.py -q` passes all unit tests
- `pytest -m regression tests/test_regression_*.py -q` passes all regression tests
- Fixture files exist: volume_profile_ohlcv.csv, order_flow_ohlcv.csv, heston_market_prices.json
- No test makes live network calls
- `make test-unit` and `make test-regression` both pass
- `tests/test_unit_ml_models.py` exists and addresses ROADMAP TEST-03 ml_models requirement
</verification>

<success_criteria>
- options_pricer, rl_models, financial_analytics, and ml_models each have unit tests (happy-path + edge case, or documented coverage justification)
- Volume Profile, Order Flow, Heston, HMM all have regression tests with pinned expected values
- Frozen fixture data files committed to tests/fixtures/
- All new tests pass with appropriate markers
</success_criteria>

<output>
After completion, create `.planning/phases/23-end-to-end-test-suite-design/23-03-SUMMARY.md`
</output>
