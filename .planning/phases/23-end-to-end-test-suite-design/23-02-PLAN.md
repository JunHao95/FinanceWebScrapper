---
phase: 23-end-to-end-test-suite-design
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - tests/test_integration_routes.py
autonomous: true
requirements:
  - TEST-04

must_haves:
  truths:
    - "Every Flask route in webapp.py has at least one integration test"
    - "Each tested route has both a happy-path test and an invalid-input test"
    - "No integration test makes a live network call — all external APIs are mocked"
    - "All integration tests pass with `pytest -m integration -q`"
  artifacts:
    - path: "tests/test_integration_routes.py"
      provides: "Integration tests for all ~22 untested Flask routes"
      min_lines: 200
  key_links:
    - from: "tests/test_integration_routes.py"
      to: "webapp.py"
      via: "Flask test client"
      pattern: "client\\.(get|post)"
---

<objective>
Write integration tests for all ~22 Flask API routes that currently lack test coverage. Each route gets a happy-path test (valid input, check 200 status + response schema) and an invalid-input test (missing/bad params, check error status).

Purpose: Ensures every API endpoint is exercised and validates both successful behavior and error handling, catching route-level regressions.
Output: tests/test_integration_routes.py with comprehensive route coverage.
</objective>

<execution_context>
@/Users/junhaotee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/junhaotee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-end-to-end-test-suite-design/23-CONTEXT.md
@.planning/phases/23-end-to-end-test-suite-design/23-RESEARCH.md
@.planning/phases/23-end-to-end-test-suite-design/23-01-SUMMARY.md
@tests/conftest.py
@webapp.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write integration tests for GET routes and simple POST routes</name>
  <files>tests/test_integration_routes.py</files>
  <action>
Create `tests/test_integration_routes.py`. Import webapp inside the client fixture (follow existing pattern — do NOT import webapp at module top level).

Use the `client` fixture from conftest.py (added in Plan 01). Every test gets `@pytest.mark.integration`.

Write tests for these routes (happy-path + invalid-input each):

**GET routes:**
- `/health` — expect 200, response has status key
- `/api/validate_ticker?ticker=AAPL` — mock yfinance.Ticker to return a mock with `.info` containing `{'shortName': 'Apple'}`. Invalid: missing ticker param.

**POST routes (no heavy compute):**
- `/api/fundamental-analysis` — mock the scraper classes. Send `{tickers: ['AAPL'], sources: ['yahoo']}`. Expect 200. Invalid: empty tickers list.
- `/api/option_pricing` — send `{S: 100, K: 100, T: 1, r: 0.05, sigma: 0.2}`. Expect 200 + price in response. Invalid: missing S.
- `/api/implied_volatility` — send valid params. Invalid: missing market_price.
- `/api/greeks` — send valid option params. Expect 200 + delta/gamma/theta/vega. Invalid: missing params.
- `/api/model_comparison` — send valid params. Expect 200. Invalid: empty body.
- `/api/convergence_analysis` — send valid params. Expect 200. Invalid: empty body.
- `/api/volatility_surface` — send valid params. Expect 200 + surface data. Invalid: empty body.
- `/api/atm_term_structure` — send valid params. Expect 200. Invalid: empty body.
- `/api/heston_price` — send standard_heston_params equivalent. Expect 200 + price. Invalid: empty body.
- `/api/heston_iv_surface` — send valid Heston params. Expect 200 + iv_grid. Invalid: missing params.
- `/api/merton_price` — send valid params including jump params. Expect 200. Invalid: missing body.

For each route:
1. Read webapp.py to find the exact expected request fields
2. Mock any external calls (yfinance, OpenAI, etc.) using `unittest.mock.patch` — place patches on the module-level import path in webapp or in the analytics module
3. Assert: status code, key response fields exist, types are correct
4. For invalid-input test: assert status code is 400, 422, or 500 (route-dependent)

IMPORTANT: Import webapp inside fixture functions, not at module top. Use `unittest.mock.patch` for mocking, not `responses` library (routes use analytics modules, not raw HTTP calls).
  </action>
  <verify>
    <automated>cd /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper && pytest tests/test_integration_routes.py -m integration -q --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>tests/test_integration_routes.py exists with tests for GET routes (/health, /api/validate_ticker) and POST routes (/api/option_pricing through /api/merton_price). All pass with `pytest -m integration`.</done>
</task>

<task type="auto">
  <name>Task 2: Write integration tests for heavy-compute and streaming routes</name>
  <files>tests/test_integration_routes.py</files>
  <action>
Append to `tests/test_integration_routes.py` (created in Task 1). Add integration tests for the remaining routes that require more complex mocking:

**Routes requiring heavy mocking:**
- `/api/scrape` — mock ALL scraper classes (YahooScraper, FinvizScraper, etc.) at the class level. Send `{tickers: ['AAPL'], sources: ['yahoo']}`. Mock scrapers to return a minimal valid result dict (use the shape from existing test_peer_comparison.py as reference). Expect 200 + results dict. Invalid: no tickers.
- `/api/send-email` — mock the email sending function. Send `{to: 'test@test.com', subject: 'Test', body: 'Test body'}`. Expect 200 or appropriate status. Invalid: missing 'to' field.
- `/api/regime_detection` — mock yfinance.download to return a DataFrame of prices. Send `{ticker: 'AAPL', start_date: '2020-01-01', end_date: '2021-01-01'}`. Expect 200 + regime data. Invalid: missing ticker.
- `/api/calibrate_heston` — mock the market data fetch. Send valid Heston params. Expect 200. Invalid: empty body.
- `/api/calibrate_heston_stream` — this is an SSE endpoint. Test that it returns content-type text/event-stream. Mock underlying data.
- `/api/calibrate_merton` — mock market data. Send valid params. Expect 200. Invalid: empty body.
- `/api/credit_risk` — send valid transition matrix and params. Expect 200 + risk metrics. Invalid: empty body.
- `/api/rl_investment_mdp` — send valid MDP params. Expect 200. Invalid: empty body.
- `/api/rl_gridworld` — send valid grid params. Expect 200. Invalid: empty body.
- `/api/rl_portfolio_rotation_pi` — send valid params. Expect 200. Invalid: empty body.
- `/api/stoch_portfolio_mdp` — send valid params. Expect 200. Invalid: empty body.
- `/api/rl_portfolio_rotation_ql` — send valid params. Expect 200. Invalid: empty body.

For each route:
1. Read webapp.py to find exact request field names and the function it calls
2. Mock at the analytics module function level (e.g., `patch('src.analytics.trading_indicators.compute_volume_profile')`) or at yfinance level
3. For SSE routes, read the first few bytes of response and check content-type header
4. Assert status code and key response fields

Keep ALL tests marked `@pytest.mark.integration`. Keep `import webapp` inside fixtures only.
  </action>
  <verify>
    <automated>cd /Users/junhaotee/Desktop/Side_projects/FinanceWebScrapper && pytest tests/test_integration_routes.py -m integration -q --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>test_integration_routes.py covers all ~22 previously untested routes. Every route has at least one happy-path and one invalid-input test. All pass green.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_integration_routes.py -m integration -q` passes all tests
- `pytest tests/test_integration_routes.py --collect-only -q | wc -l` shows >= 44 tests (22 routes x 2 tests each)
- No live network calls made (verified by running with network disabled or checking for mock patches)
</verification>

<success_criteria>
- Every Flask route in webapp.py has at least one integration test across the test suite
- Each new test has both happy-path (200) and invalid-input (4xx/5xx) coverage
- All external API calls are mocked — no live network during tests
- `make test-integration` passes green
</success_criteria>

<output>
After completion, create `.planning/phases/23-end-to-end-test-suite-design/23-02-SUMMARY.md`
</output>
